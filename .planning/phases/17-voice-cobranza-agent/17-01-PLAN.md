---
phase: 17-voice-cobranza-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_cobranza.py
autonomous: true
requirements:
  - COBR-01
  - COBR-02
  - COBR-03
  - COBR-04

must_haves:
  truths:
    - "8 xfail test stubs exist for COBR-01 through COBR-04"
    - "pytest collects test_cobranza.py without import errors before cobranza/ module exists"
    - "All stubs are marked strict=False so CI does not block on unimplemented features"
  artifacts:
    - path: "backend/tests/test_cobranza.py"
      provides: "Nyquist-compliant xfail stubs for all 4 requirements"
      min_lines: 60
  key_links:
    - from: "backend/tests/test_cobranza.py"
      to: "backend/cobranza/router.py"
      via: "lazy import inside fixture body (avoids collection-time error)"
      pattern: "import.*inside.*fixture|lazy.*import"
---

<objective>
Wave 0 test scaffold for Phase 17. Creates 8 xfail stubs (2 per requirement) covering all COBR-01 through COBR-04 behaviors — following the established Landa pattern from Phases 13, 14, and 16.

Purpose: Nyquist compliance — every task in subsequent plans has a pre-existing automated verify target. Tests fail (xfail) now and turn green as implementation lands.
Output: backend/tests/test_cobranza.py with 8 stubs, importable without cobranza/ existing.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-voice-cobranza-agent/17-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write 8 xfail stubs for COBR-01 through COBR-04</name>
  <files>backend/tests/test_cobranza.py</files>
  <behavior>
    - COBR-01a: CSV upload with valid rows returns 201 + list of created debtor IDs + error report for invalid rows
    - COBR-01b: Manual debtor creation with valid payload returns 201 + debtor with estado="pendiente"
    - COBR-02a: POST /api/cobranza/onboarding/start triggers Queen proposal and returns structured estrategia JSON
    - COBR-02b: POST /api/cobranza/onboarding/approve saves estrategia to MongoDB and returns campaign_id
    - COBR-03a: POST /api/vapi/tool-call with "consultar_deuda" tool returns HTTP 200 + result string containing monto
    - COBR-03b: POST /api/vapi/call-ended with end-of-call-report updates debtor estado and pushes debtor_update WS event
    - COBR-04a: GET /api/cobranza/debtors returns paginated debtor list filterable by estado
    - COBR-04b: GET /api/cobranza/debtors/{id} returns full debtor detail with historial_llamadas array
  </behavior>
  <action>
    Create backend/tests/test_cobranza.py following the exact xfail pattern from test_whatsapp.py (Phase 16-01):

    1. Module-level imports: pytest, pytest_asyncio. NO import of cobranza modules at module level.
    2. Copy reset_db autouse fixture from test_whatsapp.py (self-contained, not imported from conftest) — same mongomock isolation pattern.
    3. async_client fixture: lazy import `from main import app` INSIDE the fixture body (avoids collection-time ImportError before cobranza/ exists).
    4. Each stub: async def test_cobr_XX_YY(...) decorated with @pytest.mark.xfail(strict=False, reason="COBR-XX not implemented yet").
    5. Stub body: `raise NotImplementedError("COBR-XX: {behavior description}")`.

    Stub names and requirements:
    - test_cobr_01_csv_upload → COBR-01
    - test_cobr_01_manual_add → COBR-01
    - test_cobr_02_queen_propone_estrategia → COBR-02
    - test_cobr_02_approve_saves_campaign → COBR-02
    - test_cobr_03_tool_call_consultar_deuda → COBR-03
    - test_cobr_03_call_ended_updates_estado → COBR-03
    - test_cobr_04_list_debtors_filterable → COBR-04
    - test_cobr_04_debtor_detail_historial → COBR-04

    Do NOT import `from cobranza import ...` at module level — cobranza/ does not exist yet. Pattern verified in test_whatsapp.py line 1-5 (no wa_handler import at top).
  </action>
  <verify>
    <automated>cd C:/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_cobranza.py -v --tb=short 2>&amp;1 | tail -20</automated>
  </verify>
  <done>pytest collects 8 tests, all reported as XFAIL (not ERROR, not FAILED). Exit code 0.</done>
</task>

</tasks>

<verification>
cd C:/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_cobranza.py -v 2>&amp;1 | grep -E "XFAIL|PASSED|ERROR|FAILED|collected"
Expected: "8 xfailed" in summary line, exit code 0.
</verification>

<success_criteria>
- 8 tests collected, 8 xfailed, 0 errors, 0 failures
- File importable without cobranza/ package existing
- All 4 requirement IDs represented (2 stubs each)
</success_criteria>

<output>
After completion, create .planning/phases/17-voice-cobranza-agent/17-01-SUMMARY.md
</output>
