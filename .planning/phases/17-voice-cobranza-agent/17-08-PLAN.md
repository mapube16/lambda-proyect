---
phase: 17-voice-cobranza-agent
plan: 08
type: execute
wave: 5
depends_on:
  - 17-04
  - 17-05
  - 17-06
  - 17-07
files_modified:
  - backend/main.py
  - backend/requirements.txt
autonomous: false
requirements:
  - COBR-01
  - COBR-02
  - COBR-03
  - COBR-04

must_haves:
  truths:
    - "GET /api/cobranza/debtors returns 200 for authenticated client (not 404 or 422)"
    - "POST /api/vapi/tool-call returns 200 always (no JWT required)"
    - "POST /api/vapi/call-ended returns 200 always (no JWT required)"
    - "APScheduler starts 3 cobranza jobs on app startup (cobr_pre_vencimiento, cobr_post_vencimiento, cobr_rescue_llamando)"
    - "All xfail stubs in test_cobranza.py are now passing (not xfail)"
    - "Existing tests (test_auth.py, test_landa.py, test_whatsapp.py) still pass"
  artifacts:
    - path: "backend/main.py"
      provides: "cobranza router + vapi_router included; register_cobranza_jobs called in lifespan"
      contains: "register_cobranza_jobs"
    - path: "backend/requirements.txt"
      provides: "vapi_server_sdk, phonenumbers, pytz listed"
  key_links:
    - from: "backend/main.py (lifespan)"
      to: "backend/cobranza/campaign_scheduler.py"
      via: "register_cobranza_jobs(scheduler) call"
      pattern: "register_cobranza_jobs"
    - from: "backend/main.py"
      to: "backend/cobranza/router.py"
      via: "app.include_router(cobranza_router)"
      pattern: "include_router.*cobranza"
    - from: "backend/main.py"
      to: "backend/cobranza/webhooks.py"
      via: "app.include_router(vapi_router)"
      pattern: "include_router.*vapi"
---

<objective>
Wire cobranza into main.py: include routers, register scheduler jobs, verify all xfail stubs are now passing, confirm existing tests are unbroken. Final integration checkpoint.

Purpose: All four requirements COBR-01..04 become verifiably implemented. User sees working end-to-end system.
Output: main.py updated with 2 router includes + scheduler registration. All 8 test_cobranza stubs turn green.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-voice-cobranza-agent/17-CONTEXT.md
@.planning/phases/17-voice-cobranza-agent/17-05-SUMMARY.md
@.planning/phases/17-voice-cobranza-agent/17-06-SUMMARY.md
@.planning/phases/17-voice-cobranza-agent/17-07-SUMMARY.md
@backend/main.py
</context>

<interfaces>
<!-- Existing lifespan pattern in main.py -->
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    await init_db()
    start_scheduler()   # existing — from landa.scheduler
    yield
    shutdown_scheduler()
```

<!-- include_router pattern — no existing uses but FastAPI standard -->
```python
from cobranza.router import router as cobranza_router
from cobranza.webhooks import vapi_router
app.include_router(cobranza_router)
app.include_router(vapi_router)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire cobranza router + vapi webhooks + scheduler into main.py</name>
  <files>backend/main.py, backend/requirements.txt</files>
  <action>
    1. Add imports near the top of main.py (after existing imports, before app definition):
       ```python
       from cobranza.router import router as cobranza_router
       from cobranza.webhooks import vapi_router
       from cobranza.campaign_scheduler import register_cobranza_jobs
       ```

    2. In the lifespan context manager, after `start_scheduler()`:
       ```python
       from landa.scheduler import scheduler as _scheduler  # get existing scheduler instance
       register_cobranza_jobs(_scheduler)
       ```
       If the scheduler in landa/scheduler.py is exposed differently (e.g., as a module-level variable called `scheduler`), use that. Read landa/scheduler.py to verify the exact name before writing.

    3. After the `app = FastAPI(...)` instantiation (or after existing route declarations — either works):
       ```python
       app.include_router(cobranza_router)
       app.include_router(vapi_router)
       ```
       Place BEFORE the static file mount (`app.mount("/", ...)`) so API routes take priority.

    4. The Vapi webhook endpoints (/api/vapi/tool-call, /api/vapi/call-ended) must NOT require JWT auth — they are called by Vapi servers, not by users. Confirm webhooks.py does not use `Depends(get_current_user)` on these routes (it should not from Plan 05).

    5. Update backend/requirements.txt: add these lines if not already present:
       - vapi_server_sdk
       - phonenumbers
       - pytz
       - pandas

    6. Run: `pip install vapi_server_sdk phonenumbers pytz pandas` (in backend/ venv).
  </action>
  <verify>
    <automated>cd C:/Users/maxim/Desktop/hive-pixel-office/backend && python -c "from main import app; routes = [r.path for r in app.routes if hasattr(r,'path')]; cobranza = [r for r in routes if 'cobranza' in r or 'vapi' in r]; print(f'{len(cobranza)} cobranza/vapi routes')"</automated>
  </verify>
  <done>At least 12 cobranza/vapi routes registered in app. main.py imports without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement test_cobranza stubs to make them pass</name>
  <files>backend/tests/test_cobranza.py</files>
  <behavior>
    - test_cobr_01_csv_upload: POST /api/cobranza/debtors/csv with valid 2-row CSV returns 201 {created:2, errors:[]}
    - test_cobr_01_manual_add: POST /api/cobranza/debtors with {nombre, telefono, monto, vencimiento} returns 201 debtor with estado=pendiente
    - test_cobr_02_queen_propone_estrategia: POST /api/cobranza/onboarding/start with {descripcion} returns 200 {estrategia: {tono, frecuencia_dias, max_intentos, guion}}
    - test_cobr_02_approve_saves_campaign: POST /api/cobranza/onboarding/approve with {estrategia} returns 200 {campaign_id, ok:True}
    - test_cobr_03_tool_call_consultar_deuda: POST /api/vapi/tool-call with tool payload returns 200 {results:[{toolCallId, result: string}]}
    - test_cobr_03_call_ended_updates_estado: POST /api/vapi/call-ended with end-of-call-report updates debtor estado in DB
    - test_cobr_04_list_debtors_filterable: GET /api/cobranza/debtors?estado=pendiente returns list containing only pendiente debtors
    - test_cobr_04_debtor_detail_historial: GET /api/cobranza/debtors/{id} returns debtor with historial_llamadas key
  </behavior>
  <action>
    Replace each `@pytest.mark.xfail` stub with a real test. Keep the test functions self-contained using the existing async_client and reset_db fixtures from the file.

    For each test:
    1. **test_cobr_01_csv_upload**: Create a valid CSV bytes object with 2 rows (nombre,telefono,monto,vencimiento). POST to /api/cobranza/debtors/csv with files={"file": ("debtors.csv", csv_bytes, "text/csv")} and auth header. Assert 201 and created==2.

    2. **test_cobr_01_manual_add**: POST /api/cobranza/debtors with JSON body {nombre:"Juan", telefono:"+573001234567", monto:500000, vencimiento:"2026-06-01"}. Auth required — register+login a test user first. Assert 201 and debtor.estado=="pendiente".

    3. **test_cobr_02_queen_propone_estrategia**: POST /api/cobranza/onboarding/start {descripcion:"Tengo 50 deudores morosos"}. Assert 200 and response["estrategia"] has keys tono, frecuencia_dias, max_intentos, guion. (Queen returns fallback when OPENAI_API_KEY not set — this is fine for tests.)

    4. **test_cobr_02_approve_saves_campaign**: POST /api/cobranza/onboarding/approve {estrategia: {tono:"profesional", frecuencia_dias:2, max_intentos:5, guion:{saludo:"Hola", propuesta:"Tiene deuda", objeciones:"Entiendo", cierre:"Gracias"}}}. Assert 200 and response["ok"]==True.

    5. **test_cobr_03_tool_call_consultar_deuda**: Insert a test debtor directly into db.debtors. POST /api/vapi/tool-call with body {"message": {"type":"tool-calls", "call":{"id":"c1","assistantOverrides":{"variableValues":{"debtor_id":str(debtor_id)}}}, "toolWithToolCallList":[{"name":"consultar_deuda","toolCall":{"id":"tc1","parameters":{"debtor_id":str(debtor_id)}}}]}}. Assert response.status_code==200 and "results" in body.

    6. **test_cobr_03_call_ended_updates_estado**: Insert debtor with vapi_call_id="c_test". POST /api/vapi/call-ended with end-of-call-report body for call_id "c_test" and endedReason "no-answer". Assert debtor.estado=="sin_contacto" in DB after call.

    7. **test_cobr_04_list_debtors_filterable**: Insert 2 pendiente + 1 pagado debtors for test user. GET /api/cobranza/debtors?estado=pendiente. Assert response is list of length 2 and all have estado=="pendiente".

    8. **test_cobr_04_debtor_detail_historial**: Insert debtor. GET /api/cobranza/debtors/{id}. Assert "historial_llamadas" in response and it is a list.

    Auth pattern for tests: register+login in each test (following test_auth.py or test_landa.py pattern for reuse).

    Remove @pytest.mark.xfail decorators from all 8 tests once they are properly implemented.
  </action>
  <verify>
    <automated>cd C:/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_cobranza.py -v --tb=short 2>&amp;1 | tail -20</automated>
  </verify>
  <done>8 tests pass (not xfail — actual PASSED). 0 errors. Existing test suite still clean: `python -m pytest tests/ -x --ignore=tests/test_cobranza.py -q` passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete voice cobranza agent:
    - Debtor ingestion (CSV + manual) via POST /api/cobranza/debtors
    - Conversational onboarding (Queen proposes estrategia) via /api/cobranza/onboarding/
    - Vapi webhooks (/api/vapi/tool-call, /api/vapi/call-ended) for live call tool dispatch
    - APScheduler campaign jobs (pre/post-vencimiento + rescue fallback)
    - Frontend CobranzaTab with debtor table, filters, detail modal, real-time WS updates
  </what-built>
  <how-to-verify>
    1. Start backend: `cd backend && uvicorn main:app --port 8001 --reload`
       Confirm startup logs show: "cobr_pre_vencimiento", "cobr_post_vencimiento", "cobr_rescue_llamando" scheduler jobs registered.

    2. Start frontend: `cd frontend && npm run dev`
       Open http://localhost:5173, log in as a client user.

    3. Verify Cobranza tab appears in ClientDashboard navigation alongside "Leads".

    4. Click Cobranza tab — verify debtor table renders (empty state OK).

    5. Test CSV upload: create a CSV file with headers nombre,telefono,monto,vencimiento and 2 test rows:
       ```
       nombre,telefono,monto,vencimiento
       Juan Perez,+573001234567,500000,2026-06-01
       Maria Lopez,3009876543,300000,2026-05-15
       ```
       POST to http://localhost:8001/api/cobranza/debtors/csv (use curl or the UI if wired).
       Confirm 201 response with created:2.

    6. Verify debtors appear in the Cobranza tab table with estado=pendiente.

    7. Test onboarding: POST to /api/cobranza/onboarding/start {"descripcion":"Tengo deudores morosos de cartera pequeña"}.
       Confirm response has estrategia.guion with 4 sections.

    8. Verify /api/vapi/tool-call returns 200 with no auth:
       ```
       curl -X POST http://localhost:8001/api/vapi/tool-call \
         -H "Content-Type: application/json" \
         -d '{"message":{"type":"tool-calls","call":{"id":"test"},"toolWithToolCallList":[]}}'
       ```
       Should return 200 {"results":[]}.

    9. Run full test suite: `cd backend && python -m pytest tests/ -q`
       All tests must pass.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found.</resume-signal>
</task>

</tasks>

<verification>
cd C:/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/ -q 2>&amp;1 | tail -5
Expected: all passing, 0 errors.
</verification>

<success_criteria>
- All 8 test_cobranza tests PASS (not xfail)
- Existing test suites unbroken
- Backend starts with 3 cobranza scheduler jobs registered
- /api/vapi/tool-call and /api/vapi/call-ended respond 200 without JWT
- /api/cobranza/* endpoints require JWT
- Cobranza tab visible and functional in ClientDashboard
</success_criteria>

<output>
After completion, create .planning/phases/17-voice-cobranza-agent/17-08-SUMMARY.md
</output>
