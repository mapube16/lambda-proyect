---
phase: 14-landa-api-checkpoint-ui
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_landa_api.py
autonomous: true
requirements: [LANDA-09, LANDA-10, LANDA-11]

must_haves:
  truths:
    - "8 xfail stubs exist in test_landa_api.py covering LANDA-09, LANDA-10, LANDA-11"
    - "pytest collects 8 tests, all xfail, 0 errors"
    - "Each stub imports the production endpoint path that Wave 1 will implement"
  artifacts:
    - path: "backend/tests/test_landa_api.py"
      provides: "xfail test stubs for all 3 backend requirements"
      contains: "pytest.mark.xfail"
  key_links:
    - from: "backend/tests/test_landa_api.py"
      to: "backend/main.py"
      via: "httpx ASGI TestClient"
      pattern: "AsyncClient.*app=app"
---

<objective>
Create the Wave 0 xfail test scaffold for Phase 14 backend endpoints.

Purpose: Establish test contracts before implementation so Wave 1 executor has a clear target and Nyquist compliance is maintained throughout the phase.
Output: `backend/tests/test_landa_api.py` with 8 strict xfail stubs.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-landa-api-checkpoint-ui/14-CONTEXT.md
@.planning/phases/14-landa-api-checkpoint-ui/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 8 xfail stubs in test_landa_api.py</name>
  <files>backend/tests/test_landa_api.py</files>
  <action>
Create `backend/tests/test_landa_api.py` following the exact pattern from `backend/tests/test_landa.py` (import-inside-body pattern, mongomock_motor, httpx ASGI transport, asyncio_mode=auto from pytest.ini).

The file needs these exact 8 stubs (all `strict=True` xfail):

1. `test_checkpoint_returns_leads_with_canales` — GET /api/leads/checkpoint returns list with puntaje, criterios, señales, canales fields. LANDA-09.
2. `test_decision_aprobar_transitions_to_outreach` — POST /api/leads/{id}/decision {decision:"aprobar", canal_elegido:"email"} transitions estado to "outreach". LANDA-09.
3. `test_decision_rechazar_transitions_to_nurturing` — POST /api/leads/{id}/decision {decision:"rechazar", motivo:"no_fit"} transitions estado to "nurturing". LANDA-09.
4. `test_decision_pausar_transitions_to_pausado` — POST /api/leads/{id}/decision {decision:"pausar"} transitions estado to "pausado". LANDA-09.
5. `test_handover_get_returns_package` — GET /api/leads/{id}/handover returns {lead, hilo_conversacion, calificacion_original, sugerencia_cierre}. LANDA-10.
6. `test_handover_tomar_cancels_actions` — POST /api/leads/{id}/handover/tomar calls cancel_lead_actions and sets estado="handover". LANDA-10.
7. `test_reporte_mal_transitions_nurturing` — POST /api/leads/{id}/reporte-llamada {resultado:"mal", detalle:"no_interesa"} transitions to nurturing. LANDA-11.
8. `test_reporte_nopude_ocupado_schedules_retry` — POST /api/leads/{id}/reporte-llamada {resultado:"no_pude", sub_tipo:"ocupado"} schedules retry in 1 day. LANDA-11.

Test setup pattern (from test_landa.py / test_landa_pipeline.py):
```python
import pytest

@pytest.mark.xfail(strict=True, reason="LANDA-09: GET /api/leads/checkpoint not yet implemented")
@pytest.mark.asyncio
async def test_checkpoint_returns_leads_with_canales():
    # import inside body to ensure collection even if module missing
    from httpx import AsyncClient, ASGITransport
    from main import app
    raise NotImplementedError
```

All 8 stubs must use `raise NotImplementedError` as the body — they must fail, not pass.
  </action>
  <verify>
    <automated>cd /c/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_landa_api.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>pytest shows 8 xfailed, 0 passed, 0 errors. File exists at backend/tests/test_landa_api.py.</done>
</task>

</tasks>

<verification>
`cd backend && python -m pytest tests/test_landa_api.py -v` → 8 xfailed, 0 errors.
</verification>

<success_criteria>
- test_landa_api.py exists with 8 strict xfail stubs
- All stubs import production endpoint path inside the body
- pytest collects 8 tests, all xfail, 0 errors or failures
</success_criteria>

<output>
After completion, create `.planning/phases/14-landa-api-checkpoint-ui/14-01-SUMMARY.md`
</output>
