---
phase: 20-scraping-improvements
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/prospector.py
autonomous: true
requirements: [SCRAPE-02, SCRAPE-04]
must_haves:
  truths:
    - "requirements.txt contains markdownify==1.2.2 and tldextract==5.3.1"
    - "prospector.py imports markdownify and tldextract at the top of the file"
    - "tldextract PSL cache is pre-warmed with tldextract.extract('') at module level"
    - "No behavior change — all imports are additive only"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "Pinned deps for markdownify and tldextract"
      contains: "markdownify==1.2.2"
    - path: "backend/prospector.py"
      provides: "Import statements for new libraries"
      contains: "from markdownify import markdownify as md"
  key_links:
    - from: "backend/requirements.txt"
      to: "Railway build"
      via: "pip install -r requirements.txt"
      pattern: "markdownify==1.2.2"
    - from: "backend/prospector.py"
      to: "html_to_compressed_markdown() in Plan 02"
      via: "from markdownify import markdownify as md"
      pattern: "from markdownify"
---

<objective>
Add markdownify==1.2.2 and tldextract==5.3.1 to requirements.txt, then add their import statements and the tldextract PSL pre-warm call to prospector.py. No behavior changes — this is pure dependency/import setup so Plans 02 and 03 can implement against these libraries without import errors.

Purpose: Separate the "add deps" concern from "add logic" so the executor in Plan 02 can focus entirely on the new function bodies rather than also managing imports and requirements.
Output: requirements.txt with 2 new pinned lines; prospector.py with 3 new import lines and 1 pre-warm call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-scraping-improvements/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add markdownify and tldextract to requirements.txt</name>
  <files>backend/requirements.txt</files>
  <action>
Read backend/requirements.txt. Append two new lines at the end of the file:

```
markdownify==1.2.2
tldextract==5.3.1
```

Do NOT remove or alter any existing lines. Do NOT add curl_cffi — it is already installed (version 0.15.0) but not in requirements.txt yet; leave that for Plan 03 when it is actually wired in, to avoid a confusing half-committed state.

Verify the file now contains both new lines before proceeding to Task 2.
  </action>
  <verify>
    <automated>python -c "txt=open('backend/requirements.txt').read(); assert 'markdownify==1.2.2' in txt; assert 'tldextract==5.3.1' in txt; print('OK')"</automated>
  </verify>
  <done>backend/requirements.txt contains markdownify==1.2.2 and tldextract==5.3.1 on separate lines; all existing deps unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add imports and tldextract PSL pre-warm to prospector.py</name>
  <files>backend/prospector.py</files>
  <action>
Read backend/prospector.py (already read — top of file is lines 1-30).

The current import block (lines 12-19) ends with:
```python
from bs4 import BeautifulSoup
```

After the `from bs4 import BeautifulSoup` line, add these three lines:

```python
from markdownify import markdownify as md
import tldextract
from curl_cffi.requests import AsyncSession, RequestsError
```

Then, after the `logger = logging.getLogger(__name__)` line (currently line 26), add the PSL pre-warm call on its own line:

```python
tldextract.extract("")   # pre-warm Public Suffix List cache at module import
```

Do NOT remove import httpx — it is still used by discover_companies_gmaps(), discover_via_serper(), discover_via_bright_data_serp(), and enrich_via_serper_places(). httpx will be removed from scrape_url() only in Plan 03.

Do NOT add any other code — functions are added in Plans 02 and 03.
  </action>
  <verify>
    <automated>cd backend && python -c "import prospector; print('imports OK')"</automated>
  </verify>
  <done>prospector.py imports markdownify, tldextract, AsyncSession, RequestsError without error; tldextract.extract('') pre-warm executes at import time; existing behavior unchanged.</done>
</task>

</tasks>

<verification>
- `python -c "txt=open('backend/requirements.txt').read(); assert 'markdownify==1.2.2' in txt; assert 'tldextract==5.3.1' in txt"` exits 0
- `cd backend && python -c "import prospector"` exits 0 with no ImportError
- `cd backend && python -m pytest tests/ -q --tb=no` exits 0 (no regressions)
</verification>

<success_criteria>
- requirements.txt has markdownify==1.2.2 and tldextract==5.3.1 appended
- prospector.py imports markdownify as md, tldextract, AsyncSession, RequestsError
- PSL pre-warm `tldextract.extract("")` executes at module import (no cold-start delay per request)
- No existing tests fail — pure additive change
</success_criteria>

<output>
After completion, create `.planning/phases/20-scraping-improvements/20-01-SUMMARY.md`
</output>
