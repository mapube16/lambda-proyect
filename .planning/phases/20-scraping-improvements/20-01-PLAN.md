---
phase: 20-scraping-improvements
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/requirements.txt
autonomous: true
requirements: [SCRAPE-02, SCRAPE-04]
must_haves:
  truths:
    - "pip install -r backend/requirements.txt succeeds without conflicts"
    - "import markdownify; import tldextract both succeed in the Railway Python environment"
    - "tldextract.extract('') runs without network error (PSL pre-warm will be done in plan 20-02)"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "markdownify==1.2.2 and tldextract==5.3.1 pinned as dependencies"
      contains: "markdownify==1.2.2"
  key_links:
    - from: "backend/requirements.txt"
      to: "backend/prospector.py"
      via: "pip install at deploy time makes markdownify and tldextract importable"
      pattern: "markdownify==1.2.2"
---

<objective>
Add `markdownify==1.2.2` and `tldextract==5.3.1` to `backend/requirements.txt` so both packages are available in the Railway worker environment for plans 20-02 and 20-03 to import.

Purpose: Plans 20-02/20-03 cannot be implemented until these packages are declared. `curl_cffi` is already installed at 0.15.0 but is not yet pinned in requirements.txt — pin it now for reproducibility.
Output: Updated `backend/requirements.txt` with three new or updated lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-scraping-improvements/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin markdownify, tldextract, and curl_cffi in requirements.txt</name>
  <files>backend/requirements.txt</files>
  <action>
Read `backend/requirements.txt` (already done at planning time — 28 lines ending with `redis>=4.2.0,<6`).

Add the following three lines at the end of the file, after the existing `redis` line:

```
curl_cffi==0.15.0
markdownify==1.2.2
tldextract==5.3.1
```

Rationale:
- `curl_cffi==0.15.0` — already installed in dev env; pin it so Railway gets the exact version that has `chrome131` support confirmed.
- `markdownify==1.2.2` — HTML→Markdown converter; zero new heavy deps (reuses beautifulsoup4==4.14.3 already in requirements.txt). NOT Crawl4AI — Crawl4AI requires Playwright (~300 MB browser binaries) which is incompatible with the Railway ARQ worker.
- `tldextract==5.3.1` — Public Suffix List domain extractor for extract_homepage(); handles .com.co, .co.uk edge cases that urlparse fails on.

Do NOT add `playwright`, `crawl4ai`, or any browser-related packages. Do NOT modify any existing line.
  </action>
  <verify>
    <automated>cd "C:/Users/Maximiliano Pulido/lambda-proyect/backend" && python -c "import markdownify; import tldextract; import curl_cffi; print('OK')"</automated>
  </verify>
  <done>
- backend/requirements.txt contains `curl_cffi==0.15.0`, `markdownify==1.2.2`, `tldextract==5.3.1` as separate lines
- `import markdownify; import tldextract; import curl_cffi` all succeed without ImportError
- No existing line was modified or deleted
  </done>
</task>

</tasks>

<verification>
- `grep "markdownify\|tldextract\|curl_cffi" backend/requirements.txt` returns all three lines
- `python -c "import markdownify; import tldextract; import curl_cffi; print('OK')"` prints OK
</verification>

<success_criteria>
- backend/requirements.txt has exactly 3 new lines added: curl_cffi==0.15.0, markdownify==1.2.2, tldextract==5.3.1
- Both new packages import cleanly in the local Python environment
- No existing dependency was changed
</success_criteria>

<output>
After completion, create `.planning/phases/20-scraping-improvements/20-01-SUMMARY.md`
</output>
