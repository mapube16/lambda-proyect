---
phase: 25-agentic-multi-tenant-architecture
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - backend/cobranza/sub_agents/__init__.py
  - backend/cobranza/sub_agents/debtor_updater.py
  - backend/cobranza/sub_agents/whatsapp_notifier.py
  - backend/cobranza/sub_agents/identity_verifier.py
  - backend/cobranza/sub_agents/escalation_handler.py
  - backend/cobranza/cobranza_orchestrator.py
autonomous: true
requirements: [AGENT-CFG-03]
must_haves:
  truths:
    - "CobranzaOrchestrator dispatches a tool-call name to the matching sub-agent handler synchronously"
    - "Every sub-agent operation filters MongoDB by user_id (no cross-tenant access)"
    - "debtor_updater updates a debtor only when user_id matches; returns {ok, error}"
    - "whatsapp_notifier returns immediately by enqueuing an ARQ job (no >3s blocking)"
    - "identity_verifier resolves confirm/deny via regex first, LLM fallback only when ambiguous"
    - "escalation_handler sets debtor estado=escalado and pushes a dashboard WS event"
  artifacts:
    - path: "backend/cobranza/cobranza_orchestrator.py"
      provides: "Direct-dispatch orchestrator with 4 sub-agent methods"
      contains: "class CobranzaOrchestrator"
    - path: "backend/cobranza/sub_agents/debtor_updater.py"
      provides: "debtor_updater sub-agent"
      contains: "user_id"
    - path: "backend/cobranza/sub_agents/whatsapp_notifier.py"
      provides: "whatsapp_notifier sub-agent (ARQ enqueue)"
      contains: "enqueue_job"
    - path: "backend/cobranza/sub_agents/identity_verifier.py"
      provides: "identity_verifier sub-agent"
      contains: "verify_identity"
    - path: "backend/cobranza/sub_agents/escalation_handler.py"
      provides: "escalation_handler sub-agent"
      contains: "escalado"
  key_links:
    - from: "backend/cobranza/cobranza_orchestrator.py"
      to: "backend/cobranza/sub_agents/*"
      via: "direct async method dispatch by tool name"
      pattern: "self\\.(update_debtor|send_whatsapp|verify_identity|escalate)"
    - from: "backend/cobranza/sub_agents/debtor_updater.py"
      to: "db.debtors"
      via: "find_one_and_update with user_id filter"
      pattern: "user_id"
---

<objective>
Build CobranzaOrchestrator as a direct-dispatch class (NOT framework AgentOrchestrator — LLM routing adds 200-500ms, violating the 500ms TTFB target) wrapping 4 sub-agents: debtor_updater, whatsapp_notifier, identity_verifier, escalation_handler. Each enforces tenant isolation via user_id MongoDB filters.

Purpose: AGENT-CFG-03 — the voice agent (25-03) invokes these as Gemini tool calls. Returning {ok, error} dicts matches GeminiLiveLLMService result_callback semantics. Slow work (WhatsApp) is dispatched to ARQ to stay under the 3s Gemini tool-response limit (RESEARCH Pitfall 3).
Output: sub_agents package + cobranza_orchestrator.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-agentic-multi-tenant-architecture/RESEARCH.md
@.planning/phases/25-agentic-multi-tenant-architecture/25-PATTERNS.md
@backend/cobranza/voice_orchestrator.py
@backend/cobranza/debtor_crud.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Four sub-agents with user_id isolation</name>
  <read_first>
    backend/cobranza/debtor_crud.py (update_debtor lines 202-240, _utcnow/_serialize 13-23)
    backend/cobranza/claude_decision.py
    backend/cobranza/voice_router.py (lines 195-242 _process_call_ended WS push + debtor update)
    backend/arq_pool.py
    .planning/phases/25-agentic-multi-tenant-architecture/25-PATTERNS.md (sub_agents sections + Shared Patterns WS push)
  </read_first>
  <files>backend/cobranza/sub_agents/__init__.py, backend/cobranza/sub_agents/debtor_updater.py, backend/cobranza/sub_agents/whatsapp_notifier.py, backend/cobranza/sub_agents/identity_verifier.py, backend/cobranza/sub_agents/escalation_handler.py</files>
  <behavior>
    - debtor_updater.update_debtor_status(db, user_id, debtor_id, fields) -> {"ok": True, "debtor": {...}} when {_id, user_id} match; {"ok": False, "error": "not_found"} when no match for that user_id (cross-tenant write blocked).
    - whatsapp_notifier.send_whatsapp(user_id, phone, message) -> {"ok": True, "queued": True} after enqueue_job; {"ok": False, "error": ...} on missing phone/message; never awaits send completion.
    - identity_verifier.verify_identity(utterance, debtor_name) -> {"confirmed": True/False, "confidence": "high"} on regex match; LLM fallback path returns a dict with confidence "low" or "medium".
    - escalation_handler.escalate(db, user_id, debtor_id, reason) -> sets debtor estado="escalado", pushes WS event, returns {"ok": True, "estado": "escalado"}.
  </behavior>
  <action>
    Create backend/cobranza/sub_agents/__init__.py (empty package marker).
    debtor_updater.py: import _utcnow pattern; `update_debtor_status(db, user_id, debtor_id, fields)` calls db.debtors.find_one_and_update({"_id": ObjectId(debtor_id), "user_id": user_id}, {"$set": {**fields, "updated_at": _utcnow()}}, return_document=True); guard invalid ObjectId -> {"ok": False, "error": "invalid_id"}; serialize result. Push WS event via lazy `from main import manager` (non-fatal try/except per PATTERNS Shared Pattern).
    whatsapp_notifier.py: `send_whatsapp(user_id, phone, message)` validates non-empty, then `from arq_pool import get_arq_pool` -> pool.enqueue_job("send_whatsapp_job", user_id=user_id, phone=phone, message=message); returns {"ok": True, "queued": True}. Wrap in try/except returning {"ok": False, "error": str(e)[:100]}.
    identity_verifier.py: module-level compiled regex _CONFIRM_PATTERNS and _DENY_PATTERNS (Spanish, re.I) exactly as PATTERNS shows; `verify_identity(utterance, debtor_name)` async — confirm/deny short-circuit; else call `_llm_verify(utterance, debtor_name)` using gpt-4o-mini (NOT realtime), returning confidence "low"/"medium". Keep under 3s.
    escalation_handler.py: `escalate(db, user_id, debtor_id, reason)` -> update_one({"_id": ObjectId(debtor_id), "user_id": user_id}, {"$set": {"estado": "escalado", "updated_at": _utcnow()}, "$inc": {"intentos": 1}, "$push": {"historial_llamadas": {"tipo": "escalacion", "reason": reason, "ts": _utcnow()}}}); WS push via lazy `from main import manager`; return {"ok": True, "estado": "escalado"}.
    All four handlers return {"ok": bool, ...} matching GeminiLiveLLMService result_callback shape.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_cobranza_phase25.py -x -q -k "orchestrator or dispatch"</automated>
    <automated>cd backend && python -c "from cobranza.sub_agents import debtor_updater, whatsapp_notifier, identity_verifier, escalation_handler; print('import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: each sub-agent that touches db.debtors includes `"user_id": user_id` in its filter (grep confirms in all 3 of debtor_updater/escalation_handler files; identity_verifier has no DB write).
    - Source assertion: whatsapp_notifier.py contains `enqueue_job` and does NOT `await` a send_whatsapp_text call (grep `await.*send_whatsapp_text` returns 0).
    - Behavior: cross-tenant update returns {"ok": False} (debtor of user B not updatable as user A).
  </acceptance_criteria>
  <done>4 sub-agents implement {ok,error} contract; all DB ops scoped by user_id; WhatsApp is async-enqueued.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CobranzaOrchestrator direct-dispatch class</name>
  <read_first>
    backend/cobranza/voice_orchestrator.py (lines 33-67 init, 140-145 logging)
    backend/cobranza/sub_agents/ (from Task 1)
    .planning/phases/25-agentic-multi-tenant-architecture/25-PATTERNS.md (cobranza_orchestrator.py section)
    .planning/phases/25-agentic-multi-tenant-architecture/RESEARCH.md (Pattern 6 + Pitfall 6)
  </read_first>
  <files>backend/cobranza/cobranza_orchestrator.py</files>
  <behavior>
    - CobranzaOrchestrator(user_id, tenant_config) stores user_id, config, db=get_db().
    - .update_debtor(debtor_id, fields) -> delegates to debtor_updater.update_debtor_status(self.db, self.user_id, ...).
    - .send_whatsapp(phone, message) -> delegates to whatsapp_notifier.send_whatsapp(self.user_id, ...).
    - .verify_identity(utterance) -> delegates to identity_verifier.verify_identity(utterance, debtor_name from config/debtor).
    - .escalate(reason, debtor_id) -> delegates to escalation_handler.escalate(self.db, self.user_id, ...).
    - No LLM routing: dispatch is a direct method call (latency ~0).
  </behavior>
  <action>
    Create backend/cobranza/cobranza_orchestrator.py. Class CobranzaOrchestrator with `__init__(self, user_id: str, tenant_config: dict)` setting self.user_id, self.config, self.db = get_db(). Four async methods (update_debtor, send_whatsapp, verify_identity, escalate) that each call the corresponding sub-agent function, passing self.user_id. Each method logs `[CobranzaOrchestrator] dispatch %s for user %s` and returns the sub-agent dict unchanged. Add a class docstring stating this is direct dispatch (NOT framework AgentOrchestrator) to avoid the 200-500ms LLM-routing latency per RESEARCH Pitfall 6. Do NOT import or instantiate AgentRunner / AgentOrchestrator.
    Turn test_orchestrator_dispatch green.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_cobranza_phase25.py::test_orchestrator_dispatch -x -q</automated>
    <automated>cd backend && python -c "import ast; ast.parse(open('cobranza/cobranza_orchestrator.py').read()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: cobranza_orchestrator.py contains `class CobranzaOrchestrator` with 4 async methods (update_debtor, send_whatsapp, verify_identity, escalate).
    - Source assertion: no `AgentOrchestrator` or `AgentRunner` import (grep returns 0).
    - Behavior: test_orchestrator_dispatch passes — a simulated tool call routes to the correct sub-agent.
  </acceptance_criteria>
  <done>Orchestrator dispatches 4 tool names to sub-agents synchronously, user_id passed through every call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| voice LLM→orchestrator | Gemini tool calls cross into sub-agent dispatch with user_id from the call session |
| orchestrator→MongoDB | All debtor reads/writes; must be tenant-scoped |
| orchestrator→ARQ | Async WhatsApp delivery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Elevation of Privilege | sub-agent DB writes | mitigate | every db.debtors filter includes `{"user_id": user_id}`; cross-tenant write returns ok:false |
| T-25-04 | Information Disclosure | identity_verifier LLM fallback | accept | only the utterance + debtor_name are sent; no PII beyond call scope |
| T-25-05 | Denial of Service | long-running tool call freezing Gemini | mitigate | WhatsApp dispatched to ARQ; all handlers kept <3s (RESEARCH Pitfall 3) |
</threat_model>

<verification>
- `pytest tests/test_cobranza_phase25.py -x -q -k "orchestrator or dispatch"` green.
- grep confirms user_id filter on all sub-agent DB writes.
- No AgentOrchestrator/AgentRunner import in the new files.
</verification>

<success_criteria>
- CobranzaOrchestrator dispatches 4 sub-agents directly with zero LLM routing.
- All DB operations are tenant-isolated by user_id.
- WhatsApp returns immediately via ARQ enqueue.
</success_criteria>

<output>
Create `.planning/phases/25-agentic-multi-tenant-architecture/25-02-SUMMARY.md` when done.
</output>
