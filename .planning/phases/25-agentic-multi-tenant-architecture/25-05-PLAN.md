---
phase: 25-agentic-multi-tenant-architecture
plan: 05
type: execute
wave: 4
depends_on: ["25-01", "25-02", "25-03", "25-04"]
files_modified:
  - backend/routers/tenant_admin.py
  - backend/main.py
  - backend/worker.py
autonomous: false
requirements:
  - AGENT-CFG-01
  - AGENT-CFG-02
  - AGENT-CFG-03
  - VOICE-01
  - VOICE-02
  - RAG-01
  - RAG-02
  - CACHE-01

must_haves:
  truths:
    - "GET /api/tenant/config devuelve la config del tenant autenticado (solo la suya)"
    - "PATCH /api/tenant/config actualiza tenant_configs e invalida Redis inmediatamente"
    - "POST /api/tenant/modules/{module}/toggle habilita/deshabilita módulo con invalidación Redis"
    - "PATCH /api/tenant/agents/{agent_type} actualiza agent_instances y dispara append_prompt_version cuando new_prompt presente"
    - "POST /api/tenant/rag/upload ingesta documento via rag_service.ingest_document(user_id, ...)"
    - "GET /api/tenant/rag/documents lista solo los documentos del tenant autenticado"
    - "worker.py contiene job log_debtor_communication registrado en WorkerSettings.functions"
    - "main.py incluye tenant_admin router"
    - "Todos los endpoints usan Depends(get_current_user) — nunca aceptan user_id del HTTP body para rutas propias"
  artifacts:
    - path: "backend/routers/tenant_admin.py"
      provides: "7 REST endpoints para gestión de config multi-tenant + RAG upload"
      exports:
        - "GET /api/tenant/config"
        - "PATCH /api/tenant/config"
        - "POST /api/tenant/modules/{module}/toggle"
        - "GET /api/tenant/agents"
        - "PATCH /api/tenant/agents/{agent_type}"
        - "POST /api/tenant/rag/upload"
        - "GET /api/tenant/rag/documents"
    - path: "backend/main.py"
      provides: "tenant_admin router incluido en la app"
      contains: "tenant_admin"
    - path: "backend/worker.py"
      provides: "log_debtor_communication ARQ job"
      contains: "log_debtor_communication"
  key_links:
    - from: "backend/routers/tenant_admin.py"
      to: "backend/cobranza/config_cache.py"
      via: "invalidate_config llamado en PATCH + toggle"
      pattern: "invalidate_config"
    - from: "backend/routers/tenant_admin.py"
      to: "backend/cobranza/rag_service.py"
      via: "ingest_document en POST /api/tenant/rag/upload"
      pattern: "ingest_document"
    - from: "backend/worker.py"
      to: "db.debtors"
      via: "log_debtor_communication escribe historial_llamadas con user_id filter"
      pattern: "historial_llamadas"
---

<objective>
Wave 4: Tenant admin API + ARQ communication log + wiring completo.

Crea `backend/routers/tenant_admin.py` con 7 endpoints REST para que cada tenant gestione su propia configuración (modules, brand_name, language, voice_system_prompt), instancias de agentes (model, temperature, tools, historial de prompts), y RAG (upload de documentos, listado). Modifica `backend/main.py` para incluir el router. Agrega `log_debtor_communication` como ARQ job en `backend/worker.py`.

Purpose: Este plan cierra el ciclo completo de Phase 25 — los tenants pueden ahora modificar su config desde la API, los cambios se invalidan de Redis inmediatamente (CACHE-01), y los sub-agents tienen un canal para loggear comunicaciones asíncronamente via ARQ (AGENT-CFG-03). El human-verify checkpoint confirma que el hot-reload end-to-end funciona desde la API hasta la voz.

Output: `backend/routers/tenant_admin.py` (nuevo), `backend/main.py` (modificado), `backend/worker.py` (modificado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-agentic-multi-tenant-architecture/RESEARCH.md
@.planning/phases/25-agentic-multi-tenant-architecture/25-PATTERNS.md
@.planning/phases/25-agentic-multi-tenant-architecture/25-01-SUMMARY.md
@.planning/phases/25-agentic-multi-tenant-architecture/25-02-SUMMARY.md
@.planning/phases/25-agentic-multi-tenant-architecture/25-03-SUMMARY.md
@.planning/phases/25-agentic-multi-tenant-architecture/25-04-SUMMARY.md
@backend/routers/knowledge.py
@backend/routers/prospect.py
@backend/worker.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear backend/routers/tenant_admin.py (7 endpoints)</name>
  <files>backend/routers/tenant_admin.py</files>
  <behavior>
    - GET /api/tenant/config → retorna tenant_config del user autenticado; 200 con dict, {} si no existe
    - PATCH /api/tenant/config → llama upsert_tenant_config(user_id, data) + invalidate_config(user_id); retorna {"ok": True}
    - POST /api/tenant/modules/{module}/toggle → llama toggle_module(user_id, module, enabled) + invalida Redis; retorna {"ok": True, "module": module, "enabled": enabled}
    - GET /api/tenant/agents → retorna lista de agent_instances del user, o [] si ninguno
    - PATCH /api/tenant/agents/{agent_type} → llama upsert_agent_instance(user_id, agent_type, data); si data.new_prompt present, también llama append_prompt_version(user_id, agent_type, new_prompt); retorna {"ok": True}
    - POST /api/tenant/rag/upload (multipart) → para cada file: extrae texto (bytes.decode o usa rag.extract_text), llama ingest_document(user_id, content, title=filename, doc_type="file"); retorna lista de {"filename": str, "doc_id": str, "chunk_count": int} o {"filename": str, "error": str}
    - GET /api/tenant/rag/documents → llama get_rag_documents(user_id); retorna lista
    - SEGURIDAD CRÍTICA: todos los endpoints derivan user_id de `current_user = Depends(get_current_user); user_id = str(current_user["user_id"])` — NUNCA aceptan user_id del request body para operaciones sobre la config propia
  </behavior>
  <read_first>
    @.planning/phases/25-agentic-multi-tenant-architecture/25-PATTERNS.md (sección "backend/routers/tenant_admin.py", Pydantic request models, auth guard pattern, file upload pattern, error handling)
    @backend/routers/knowledge.py (líneas 1-60: imports, upload pattern, auth via require_staff)
    @backend/routers/prospect.py (líneas 1-65: get_current_user pattern, Pydantic models, error handling)
  </read_first>
  <action>
Crear `backend/routers/tenant_admin.py` nuevo con las siguientes especificaciones:

**Imports requeridos:**
```python
import logging
import asyncio
from typing import List, Optional
from datetime import datetime, timezone
from fastapi import APIRouter, HTTPException, Depends, UploadFile, File
from pydantic import BaseModel, Field
from auth import get_current_user
from cobranza.tenant_config import (
    get_tenant_config, upsert_tenant_config, toggle_module,
    get_agent_instance, upsert_agent_instance, append_prompt_version,
    get_rag_documents
)
from cobranza.config_cache import get_tenant_config as get_cached_tenant_config, invalidate_config
```

**Router:**
```python
router = APIRouter(prefix="/api/tenant", tags=["tenant-admin"])
logger = logging.getLogger("tenant_admin")
```

**Pydantic models (ASVS V5 — max_length en campos de texto libre, per D-10 seguridad):**
- `class TenantConfigUpdateRequest(BaseModel)`: `modules: Optional[dict] = None`, `voice_system_prompt: Optional[str] = Field(None, max_length=2000)`, `brand_name: Optional[str] = Field(None, max_length=200)`, `language: Optional[str] = None`
- `class AgentInstanceUpdateRequest(BaseModel)`: `model: Optional[str] = None`, `temperature: Optional[float] = None`, `tools_enabled: Optional[list[str]] = None`, `new_prompt: Optional[str] = Field(None, max_length=2000)`
- `class ModuleToggleRequest(BaseModel)`: `enabled: bool`

**Endpoints (todos con auth):**

1. `GET /config`:
```python
@router.get("/config")
async def get_my_config(current_user: dict = Depends(get_current_user)):
    user_id = str(current_user["user_id"])
    config = await get_cached_tenant_config(user_id)
    return config or {}
```

2. `PATCH /config`:
```python
@router.patch("/config")
async def update_my_config(request: TenantConfigUpdateRequest, current_user: dict = Depends(get_current_user)):
    user_id = str(current_user["user_id"])
    update_data = {k: v for k, v in request.model_dump().items() if v is not None}
    if not update_data:
        raise HTTPException(400, "No fields to update")
    await upsert_tenant_config(user_id, update_data)
    # upsert_tenant_config ya llama invalidate_config internamente
    return {"ok": True}
```

3. `POST /modules/{module}/toggle`:
```python
@router.post("/modules/{module}/toggle")
async def toggle_my_module(module: str, request: ModuleToggleRequest, current_user: dict = Depends(get_current_user)):
    user_id = str(current_user["user_id"])
    await toggle_module(user_id, module, request.enabled)
    # toggle_module ya llama invalidate_config internamente
    return {"ok": True, "module": module, "enabled": request.enabled}
```

4. `GET /agents`:
```python
@router.get("/agents")
async def list_my_agents(current_user: dict = Depends(get_current_user)):
    user_id = str(current_user["user_id"])
    from database import get_db
    db = get_db()
    cursor = db.agent_instances.find({"user_id": user_id})
    docs = []
    async for doc in cursor:
        doc["_id"] = str(doc["_id"])
        docs.append(doc)
    return docs
```

5. `PATCH /agents/{agent_type}`:
```python
@router.patch("/agents/{agent_type}")
async def update_my_agent(agent_type: str, request: AgentInstanceUpdateRequest, current_user: dict = Depends(get_current_user)):
    user_id = str(current_user["user_id"])
    update_data = {k: v for k, v in request.model_dump(exclude={"new_prompt"}).items() if v is not None}
    if update_data:
        await upsert_agent_instance(user_id, agent_type, update_data)
    if request.new_prompt:
        await append_prompt_version(user_id, agent_type, request.new_prompt)
    return {"ok": True}
```

6. `POST /rag/upload` (multipart):
```python
@router.post("/rag/upload")
async def upload_rag_document(files: List[UploadFile] = File(...), current_user: dict = Depends(get_current_user)):
    user_id = str(current_user["user_id"])
    from cobranza.rag_service import ingest_document
    results = []
    for file in files:
        file_bytes = await file.read()
        if not file_bytes:
            results.append({"filename": file.filename, "error": "empty file"})
            continue
        try:
            try:
                content = file_bytes.decode("utf-8")
            except UnicodeDecodeError:
                from rag import extract_text
                content = extract_text(file_bytes, file.filename or "upload", file.content_type or "")
            result = await ingest_document(user_id, content, title=file.filename or "upload", doc_type="file")
            results.append({"filename": file.filename, **result})
        except Exception as e:
            logger.error("[tenant_admin] RAG upload failed for %s: %s", file.filename, e)
            results.append({"filename": file.filename, "error": str(e)[:100]})
    return results
```

7. `GET /rag/documents`:
```python
@router.get("/rag/documents")
async def list_my_rag_documents(current_user: dict = Depends(get_current_user)):
    user_id = str(current_user["user_id"])
    docs = await get_rag_documents(user_id)
    return docs
```

**Error handling estándar (per PATTERNS Shared Patterns):**
- Envuelve cada endpoint en try/except; re-raise HTTPException sin envolver; loggea otros con `exc_info=True`.

SEGURIDAD: No se expone ningún endpoint que permita que un usuario modifique la config de otro usuario. `user_id` siempre viene de `Depends(get_current_user)`, nunca del request body.
  </action>
  <verify>
    <automated>cd backend && python -c "
import ast
src = open('routers/tenant_admin.py').read()
ast.parse(src)
assert 'get_current_user' in src, 'MISSING: get_current_user'
assert 'Depends(get_current_user)' in src, 'MISSING: Depends(get_current_user)'
assert 'invalidate_config' in src or 'toggle_module' in src, 'MISSING: cache invalidation'
assert 'ingest_document' in src, 'MISSING: ingest_document'
assert '/api/tenant/config' in src or 'prefix=' in src, 'MISSING: route'
print('OK: tenant_admin.py source assertions pass')
"</automated>
    <automated>cd backend && python -c "from routers.tenant_admin import router; print('OK: router imports')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/routers/tenant_admin.py` existe con 7 endpoints (`GET /config`, `PATCH /config`, `POST /modules/{module}/toggle`, `GET /agents`, `PATCH /agents/{agent_type}`, `POST /rag/upload`, `GET /rag/documents`)
    - Todos los endpoints tienen `Depends(get_current_user)`
    - PATCH /config y POST /modules/*/toggle invocan invalidación de cache (ya sea directamente o via las funciones de tenant_config.py que lo hacen internamente)
    - `voice_system_prompt` y `new_prompt` tienen `max_length=2000` en Pydantic (ASVS V5)
    - Sin endpoints que acepten `user_id` externo para ops del tenant propio
  </acceptance_criteria>
  <done>
    7 endpoints funcionales. Config PATCH invalida Redis. RAG upload delega a rag_service. Todos con Depends(get_current_user).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wiring en main.py + log_debtor_communication ARQ job en worker.py</name>
  <files>backend/main.py, backend/worker.py</files>
  <read_first>
    @backend/main.py (buscar sección donde se incluyen routers — patrón `app.include_router`)
    @backend/worker.py (líneas 130-148: WorkerSettings.functions lista + on_startup)
    @backend/arq_pool.py (create_arq_pool, redis_settings_from_url — patrón para pool en main)
  </read_first>
  <action>
**Modificar `backend/main.py`:**
Localizar donde se incluyen los demás routers (patrón `app.include_router(...)` para los routers existentes como `prospect`, `knowledge`, `whatsapp`, `cobranza`). Agregar al mismo bloque:
```python
from routers.tenant_admin import router as tenant_admin_router
app.include_router(tenant_admin_router)
```
No modificar ningún otro router existente. No cambiar el orden de los otros routers.

**Modificar `backend/worker.py`:**
Agregar una nueva función ARQ job `log_debtor_communication` después de `run_prospecting_job` y antes de `on_startup`:

```python
async def log_debtor_communication(
    ctx: dict,
    user_id: str,
    debtor_id: str,
    channel: str,          # "whatsapp" | "voice" | "sms"
    direction: str,        # "outbound" | "inbound"
    content: str,
    result: str = "",
) -> dict:
    """ARQ job: registra una comunicación en historial_llamadas del deudor.
    Llamado async desde sub-agents (whatsapp_notifier, etc.) para no bloquear tool calls.
    Filtra por user_id para garantizar tenant isolation.
    """
    import database
    from bson import ObjectId
    from datetime import datetime, timezone

    try:
        db = database.get_db()
        record = {
            "tipo": f"{channel}_{direction}",
            "channel": channel,
            "direction": direction,
            "content": content[:500],
            "result": result,
            "ts": datetime.now(timezone.utc),
        }
        try:
            oid = ObjectId(debtor_id)
        except Exception:
            logger.warning("[log_comm] Invalid debtor_id: %s", debtor_id)
            return {"ok": False, "error": "invalid_debtor_id"}

        await db.debtors.update_one(
            {"_id": oid, "user_id": user_id},  # user_id filter: tenant isolation
            {
                "$push": {"historial_llamadas": record},
                "$set": {"updated_at": datetime.now(timezone.utc)},
            },
        )
        logger.info("[log_comm] Communication logged: user=%s debtor=%s channel=%s", user_id, debtor_id, channel)
        return {"ok": True}
    except Exception as exc:
        logger.error("[log_comm] Failed: %s", exc)
        return {"ok": False, "error": str(exc)[:100]}
```

Agregar `log_debtor_communication` a `WorkerSettings.functions`:
```python
class WorkerSettings:
    functions = [run_prospecting_job, log_debtor_communication]  # agrega log_debtor_communication
    ...
```
(mantener todos los campos existentes intactos: on_startup, on_shutdown, redis_settings, max_jobs, job_timeout, keep_result, max_tries)
  </action>
  <verify>
    <automated>cd backend && python -c "
import ast
main_src = open('main.py').read()
assert 'tenant_admin' in main_src, 'MISSING: tenant_admin router in main.py'
worker_src = open('worker.py').read()
assert 'log_debtor_communication' in worker_src, 'MISSING: log_debtor_communication in worker.py'
assert 'log_debtor_communication' in worker_src.split('WorkerSettings')[1], 'MISSING: log_debtor_communication in WorkerSettings.functions'
print('OK: main.py + worker.py wiring assertions pass')
"</automated>
    <automated>cd backend && python -c "import worker; print('OK: worker imports cleanly')"</automated>
  </verify>
  <acceptance_criteria>
    - `main.py` contiene `from routers.tenant_admin import` y `app.include_router(tenant_admin_router)` (o equivalente)
    - `worker.py` contiene función `async def log_debtor_communication` con filtro `{"user_id": user_id}` en db.debtors query
    - `WorkerSettings.functions` incluye `log_debtor_communication`
    - `worker.py` parsea sin errores de importación (`python -c "import worker"` exitoso)
    - Ningún router existente fue modificado o removido en `main.py`
  </acceptance_criteria>
  <done>
    tenant_admin router activo. log_debtor_communication disponible como ARQ job para WhatsApp notifier y otros sub-agents que necesitan loggear comunicaciones asíncronamente.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Arquitectura completa Phase 25: MongoDB tenant_configs + Redis cache con invalidación inmediata + CobranzaOrchestrator + 4 sub-agents + Telnyx transport + Gemini Live LLM + RAG con Pinecone namespace isolation + Admin API completa.
  </what-built>
  <how-to-verify>
    **Test 1 — Config hot-reload:**
    1. Autenticarse con un usuario test. Hacer `PATCH /api/tenant/config` con `{"voice_system_prompt": "Hola soy {brand_name}", "brand_name": "Empresa Test"}`.
    2. Verificar `GET /api/tenant/config` retorna los valores guardados.
    3. Hacer `POST /api/tenant/modules/voice/toggle` con `{"enabled": false}`.
    4. En server logs: confirmar que el próximo intento de llamada cierra el WS con code 1008 (voice module disabled).

    **Test 2 — RAG upload:**
    1. `POST /api/tenant/rag/upload` con un archivo .txt de prueba (cualquier texto).
    2. Verificar respuesta contiene `doc_id` y `chunk_count > 0`.
    3. `GET /api/tenant/rag/documents` retorna el documento recién subido.

    **Test 3 — Agent instance update:**
    1. `PATCH /api/tenant/agents/cobranza` con `{"model": "gemini-2.0-flash-exp", "new_prompt": "Nueva versión del prompt v1"}`.
    2. Hacer el mismo PATCH 6 veces más con different prompts.
    3. Verificar en MongoDB que `agent_instances.prompt_history` contiene exactamente 5 entradas (las últimas 5), no más.

    **Test 4 — ARQ worker job:**
    1. Verificar que el ARQ worker arranca sin errores (`arq worker.WorkerSettings`).
    2. Confirmar en logs que `log_debtor_communication` aparece como función registrada.

    **Observar:**
    - Todos los endpoints retornan 401 sin JWT
    - Los endpoints 200 OK con JWT válido
    - Redis invalida cache en < 100ms después de PATCH
  </how-to-verify>
  <resume-signal>
    Escribe "approved" si los 4 tests pasan. O describe el fallo: "test-1-fails: [descripción]", "test-2-fails: [descripción]", etc.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client→/api/tenant/* | Requiere JWT válido; user_id extraído del token, nunca del request body |
| tenant_admin→Redis | Invalidación de cache en write — Redis side-channel non-issue (server-to-server) |
| tenant_admin→Pinecone | Ingest de documentos; namespace siempre = user_id del JWT |
| ARQ worker→MongoDB | log_debtor_communication siempre filtra por user_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-12 | Elevation of Privilege | PATCH /api/tenant/config cross-tenant | mitigate | user_id from Depends(get_current_user) never from request; no path param user_id in config endpoints |
| T-25-13 | Tampering | voice_system_prompt injection via API | mitigate | Pydantic max_length=2000; validator rejects overlength input with 422 |
| T-25-14 | Information Disclosure | GET /api/tenant/rag/documents listing other tenant | mitigate | get_rag_documents(user_id) always filters by user_id; no admin bypass endpoint |
| T-25-15 | Elevation of Privilege | log_debtor_communication ARQ job cross-tenant | mitigate | db.debtors.update_one filter always includes {"user_id": user_id}; returns ok:False on no-match |
| T-25-SC | Tampering | npm/pip installs | mitigate | No new installs in this plan — all deps handled in 25-01 |
</threat_model>

<verification>
```bash
cd backend && python -c "
src_main = open('main.py').read()
src_worker = open('worker.py').read()
src_admin = open('routers/tenant_admin.py').read()

# main.py wiring
assert 'tenant_admin' in src_main, 'main.py: MISSING tenant_admin router'

# worker.py new job
assert 'log_debtor_communication' in src_worker, 'worker.py: MISSING log_debtor_communication'
assert 'user_id' in src_worker.split('log_debtor_communication')[1].split('async def')[0], 'worker.py: MISSING user_id filter in log_debtor_communication'

# tenant_admin.py security
assert src_admin.count('Depends(get_current_user)') >= 7, 'tenant_admin: MISSING Depends on some endpoints'
assert 'max_length=2000' in src_admin, 'tenant_admin: MISSING max_length validation'
print('Phase 25-05: all wiring assertions PASS')
"
```

```bash
cd backend && pytest tests/test_cobranza_phase25.py -x -q
```
</verification>

<success_criteria>
- 7 endpoints activos bajo `/api/tenant/` — todos autenticados con JWT
- PATCH /api/tenant/config invalida Redis inmediatamente (CACHE-01 end-to-end)
- POST /api/tenant/modules/voice/toggle=false bloquea próxima llamada de voz (VOICE-01/VOICE-02 completo)
- PATCH /api/tenant/agents/{type} con new_prompt actualiza historial limitado a 5 (AGENT-CFG-02)
- POST /api/tenant/rag/upload delega a rag_service.ingest_document con user_id correcto (RAG-01/RAG-02)
- log_debtor_communication en WorkerSettings.functions (AGENT-CFG-03)
- main.py incluye tenant_admin router
- Suite de tests de Phase 25 pasa (o xfail — cero ERROR)
</success_criteria>

<output>
Crear `.planning/phases/25-agentic-multi-tenant-architecture/25-05-SUMMARY.md` cuando termine.
</output>
