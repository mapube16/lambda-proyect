---
phase: 25-agentic-multi-tenant-architecture
plan: 03
type: execute
wave: 3
depends_on: ["25-01", "25-02"]
files_modified:
  - backend/cobranza/voice_pipecat.py
  - backend/cobranza/voice_router.py
autonomous: false
requirements: [VOICE-01, VOICE-02, AGENT-CFG-01]
must_haves:
  truths:
    - "voice pipeline uses TelnyxFrameSerializer at 8kHz PCMU (not TwilioFrameSerializer at 24kHz)"
    - "voice pipeline uses GeminiLiveLLMService (not OpenAIRealtimeLLMService)"
    - "system_prompt is loaded from tenant_configs via Redis cache at call start (hot-reload)"
    - "modules.voice=false closes the WebSocket with code 1008 before the pipeline starts"
    - "webhook returns TeXML <Connect><Stream> for Telnyx; outbound call uses Telnyx Call Control"
    - "sub-agent tool calls are registered as Gemini functions (end_call + 4 sub-agents)"
  artifacts:
    - path: "backend/cobranza/voice_pipecat.py"
      provides: "Telnyx + Gemini Live pipeline with hot-reload prompt"
      contains: "TelnyxFrameSerializer"
    - path: "backend/cobranza/voice_router.py"
      provides: "TeXML webhook + Telnyx outbound initiation"
      contains: "TELNYX"
  key_links:
    - from: "backend/cobranza/voice_pipecat.py"
      to: "backend/cobranza/config_cache.py"
      via: "get_tenant_config(user_id) at call start"
      pattern: "get_tenant_config"
    - from: "backend/cobranza/voice_pipecat.py"
      to: "backend/cobranza/cobranza_orchestrator.py"
      via: "register_function dispatching to orchestrator methods"
      pattern: "register_function"
    - from: "backend/cobranza/voice_pipecat.py"
      to: "GeminiLiveLLMService"
      via: "pipeline LLM service"
      pattern: "GeminiLiveLLMService"
---

<objective>
Swap the voice pipeline transport (Twilio→Telnyx) and LLM (OpenAI Realtime→Gemini Live), and wire hot-reload of voice_system_prompt from tenant_configs via the Redis cache. Register the 4 CobranzaOrchestrator sub-agents plus end_call as Gemini function calls.

Purpose: VOICE-01 (Telnyx replaces Twilio), VOICE-02 (Gemini Live replaces OpenAI Realtime, function calling, <500ms TTFB), AGENT-CFG-01 (prompt change reflects on next call without redeploy).
Output: modified voice_pipecat.py + voice_router.py. Includes a human-verify checkpoint for a real call (audio quality + TTFB).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-agentic-multi-tenant-architecture/RESEARCH.md
@.planning/phases/25-agentic-multi-tenant-architecture/25-PATTERNS.md
@backend/cobranza/voice_pipecat.py
@backend/cobranza/voice_router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Telnyx transport + Gemini Live LLM + hot-reload prompt in voice_pipecat.py</name>
  <read_first>
    backend/cobranza/voice_pipecat.py (full file — imports 25-31, transport block ~205-220, llm block ~244-270, end_call handler ~321-339)
    backend/cobranza/config_cache.py (from 25-01)
    backend/cobranza/cobranza_orchestrator.py (from 25-02)
    .planning/phases/25-agentic-multi-tenant-architecture/25-PATTERNS.md (voice_pipecat.py MODIFY section, Changes 1-5)
    .planning/phases/25-agentic-multi-tenant-architecture/RESEARCH.md (Pattern 2, Pattern 3, Pitfall 1, Pitfall 2, Pitfall 3)
  </read_first>
  <files>backend/cobranza/voice_pipecat.py</files>
  <action>
    Apply the 5 surgical changes from PATTERNS voice_pipecat.py MODIFY:
    Change 1 (imports): replace `from pipecat.serializers.twilio import TwilioFrameSerializer` with `from pipecat.serializers.telnyx import TelnyxFrameSerializer`; replace `from pipecat.services.openai.realtime.llm import OpenAIRealtimeLLMService` and the `events as rt_events` import with `from pipecat.services.google.gemini_live.llm import GeminiLiveLLMService`; add `from cobranza.config_cache import get_tenant_config`; add `from cobranza.cobranza_orchestrator import CobranzaOrchestrator`.
    Change 4 (hot-reload, place BEFORE transport): `tenant_config = await get_tenant_config(user_id)`; if not tenant_config.get("modules", {}).get("voice", True): `await websocket.close(1008, "Voice module disabled"); return CallResult()`. Build system_prompt from `tenant_config.get("voice_system_prompt") or _build_default_prompt(...)`; apply `str.replace("{brand_name}", tenant_config.get("brand_name","nuestra empresa"))` and `str.replace("{debtor_name}", debtor.get("nombre",""))` — NO template engine (locked decision).
    Change 2 (transport): replace FastAPIWebsocketParams block — set audio_in_sample_rate=8000 and audio_out_sample_rate=8000 (CRITICAL: Telnyx 8kHz, not 24kHz — RESEARCH Pitfall 2), serializer=TelnyxFrameSerializer(stream_id=stream_id, outbound_encoding="PCMU", inbound_encoding="PCMU", call_control_id=call_control_id, api_key=os.getenv("TELNYX_API_KEY")).
    Change 3 (LLM): replace OpenAIRealtimeLLMService block with GeminiLiveLLMService(api_key=os.getenv("GOOGLE_API_KEY"), system_instruction=system_prompt, tools=[...5 tool schemas...], params=GeminiLiveLLMService.InputParams(voice_id="Charon", language_code="es-419")). Register functions: end_call, update_debtor, send_whatsapp, verify_identity, escalate. Each handler instantiates/uses a CobranzaOrchestrator(user_id, tenant_config) and calls the matching method, then `await params.result_callback(result_dict)`. Keep every handler <3s (RESEARCH Pitfall 3).
    Change 5 (end_call): remove twilio.rest.Client hang-up; TelnyxFrameSerializer with api_key handles hang-up; handler does result_callback then queues EndFrame after a short sleep.
    Accept that stream_id/call_control_id come from the websocket handshake (see Task 2 / voice_router parse).
  </action>
  <verify>
    <automated>cd backend && python -c "from pipecat.serializers.telnyx import TelnyxFrameSerializer; from pipecat.services.google.gemini_live.llm import GeminiLiveLLMService; print('imports ok')"</automated>
    <automated>cd backend && pytest tests/test_cobranza_phase25.py::test_telnyx_serializer tests/test_cobranza_phase25.py::test_gemini_live_import -x -q</automated>
    <automated>cd backend && python -c "import ast; ast.parse(open('cobranza/voice_pipecat.py').read()); print('parse ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: voice_pipecat.py contains `TelnyxFrameSerializer` and `GeminiLiveLLMService`; grep `TwilioFrameSerializer` returns 0 and `OpenAIRealtimeLLMService` returns 0.
    - Source assertion: `audio_in_sample_rate=8000` and `audio_out_sample_rate=8000` present; grep `24000` returns 0.
    - Source assertion: `get_tenant_config(` called and `websocket.close(1008` present for disabled voice module.
    - Source assertion: 5 `register_function` calls (end_call, update_debtor, send_whatsapp, verify_identity, escalate).
  </acceptance_criteria>
  <done>Pipeline runs Telnyx+Gemini at 8kHz; prompt hot-reloads from tenant_configs; voice toggle closes the socket; sub-agent tools registered.</done>
</task>

<task type="auto">
  <name>Task 2: TeXML webhook + Telnyx outbound + websocket handshake in voice_router.py</name>
  <read_first>
    backend/cobranza/voice_router.py (full file — webhook 38-56, ws handler, initiate_call_v2 ~276-299)
    .planning/phases/25-agentic-multi-tenant-architecture/25-PATTERNS.md (voice_router.py MODIFY section, Changes 1-3 + Notas críticas #1)
    .planning/phases/25-agentic-multi-tenant-architecture/RESEARCH.md (Pattern 2 TeXML, Open Question 1 parse_telephony_websocket, Open Question 3 Telnyx SDK)
  </read_first>
  <files>backend/cobranza/voice_router.py</files>
  <action>
    Change 1 (webhook): replace twiml_webhook with telnyx_webhook returning TeXML — `<?xml ...?><Response><Connect><Stream url="wss://{host}/api/cobranza/voice/ws/{call_control_id}" bidirectionalMode="rtp"/></Connect><Pause length="40"/></Response>` as PlainTextResponse media_type="application/xml". Read call_control_id from form.
    Change 3 (websocket handshake): first attempt `from pipecat.runner.utils import parse_telephony_websocket` and `transport_type, call_data = await parse_telephony_websocket(websocket)`; extract stream_id=call_data["stream_id"], call_control_id=call_data["call_control_id"]. If that import is NOT available in pipecat 0.0.108 (RESEARCH Open Question 1), keep the existing manual receive loop but adapt field names from Twilio `streamSid` to Telnyx `stream_id`/`call_control_id`. Pass both into run_bot.
    Change 2 (outbound initiate_call_v2): replace twilio.rest.Client + client.calls.create with `import telnyx; telnyx.api_key = os.getenv("TELNYX_API_KEY")` then `call = await asyncio.to_thread(telnyx.Call.create, connection_id=os.getenv("TELNYX_CONNECTION_ID"), to=to_number, from_=os.getenv("TELNYX_VOICE_PHONE_NUMBER"), webhook_url=f"{webhook_url}/api/cobranza/voice/webhook")`; capture `call.call_control_id`. Verify the exact Telnyx SDK method signature against the installed package before finalizing (RESEARCH Open Question 3) — if `telnyx.Call.create` differs, use the REST Call Control equivalent and note it in the SUMMARY.
    Keep the same FastAPI route prefixes and Depends(get_current_user) on initiate-v2.
  </action>
  <verify>
    <automated>cd backend && python -c "import ast; ast.parse(open('cobranza/voice_router.py').read()); print('parse ok')"</automated>
    <automated>cd backend && python -c "import telnyx; print('telnyx', telnyx.__version__ if hasattr(telnyx,'__version__') else 'ok')"</automated>
    <automated>cd backend && pytest tests/test_cobranza_phase25.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: voice_router.py webhook returns a string containing `<Connect>` and `<Stream` and `bidirectionalMode`; grep `VoiceResponse` returns 0.
    - Source assertion: initiate path uses `telnyx` and reads TELNYX_CONNECTION_ID / TELNYX_VOICE_PHONE_NUMBER; grep `twilio.rest` returns 0.
    - Source assertion: websocket handler references `stream_id` and `call_control_id` (not `streamSid`).
  </acceptance_criteria>
  <done>Webhook serves TeXML; outbound uses Telnyx Call Control; ws handshake yields stream_id + call_control_id.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Telnyx + Gemini Live voice pipeline with hot-reloaded system prompt from tenant_configs. A real outbound cobranza call now runs entirely on Telnyx transport and Gemini Live LLM, with 4 sub-agent tools and end_call registered.</what-built>
  <how-to-verify>
    1. Set GOOGLE_API_KEY, TELNYX_API_KEY, TELNYX_CONNECTION_ID, TELNYX_VOICE_PHONE_NUMBER in backend/.env and ensure UPSTASH_REDIS_URL (or REDIS_URL) is reachable.
    2. Seed a tenant_configs doc for your test user_id with modules.voice=true and a custom voice_system_prompt containing "{brand_name}".
    3. Start API + worker (see memory backend-dev-runbook), trigger POST /api/cobranza/voice/call/initiate-v2 with a test debtor whose phone is your own number.
    4. Answer the call. Confirm: (a) audio is clear, not garbled/silent (validates 8kHz PCMU), (b) the agent greets using your brand_name (validates hot-reload + string.replace), (c) first agent utterance arrives quickly (eyeball <500ms TTFB target — note actual).
    5. While on a second call attempt, set modules.voice=false via PATCH (25-05) or directly in MongoDB + invalidate cache; confirm the next call's WebSocket closes (voice disabled).
    6. Check server logs for the outbound call SID / call_control_id format to confirm it is Telnyx, not Twilio.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (garbled audio, wrong language/voice, TTFB too high, prompt not hot-reloading).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telnyx PSTN→webhook | Untrusted provider POST initiates the stream |
| Telnyx→WebSocket | Inbound audio frames |
| tenant_configs→pipeline | system_prompt injected into LLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06 | Tampering | voice_system_prompt injection | mitigate | prompt length capped at 2000 chars at write time (25-05 Pydantic); only string.replace of 2 known vars |
| T-25-07 | Spoofing | unauthenticated webhook POST | accept | Telnyx webhook signing deferred; endpoint only returns static TeXML, no state mutation |
| T-25-08 | Information Disclosure | wrong user_id loading another tenant's prompt | mitigate | user_id derived from the debtor record tied to the initiating tenant; get_tenant_config keyed by that user_id |
</threat_model>

<verification>
- Imports for TelnyxFrameSerializer + GeminiLiveLLMService resolve (deps from 25-01).
- grep gates confirm Twilio/OpenAI-Realtime/24000 fully removed.
- Human-verify checkpoint: real call audio clear, brand_name greeting, Telnyx SID, voice toggle closes socket.
</verification>

<success_criteria>
- Voice runs on Telnyx 8kHz + Gemini Live with 5 registered tools.
- voice_system_prompt hot-reloads from tenant_configs on next call (no redeploy).
- modules.voice=false stops new calls within 1 request.
- Outbound call uses Telnyx, confirmed by SID format.
</success_criteria>

<output>
Create `.planning/phases/25-agentic-multi-tenant-architecture/25-03-SUMMARY.md` when done.
</output>
