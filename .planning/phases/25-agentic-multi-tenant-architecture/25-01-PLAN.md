---
phase: 25-agentic-multi-tenant-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/.env.example
  - backend/database.py
  - backend/cobranza/config_cache.py
  - backend/cobranza/tenant_config.py
  - backend/tests/test_cobranza_phase25.py
autonomous: true
requirements: [AGENT-CFG-01, AGENT-CFG-02, CACHE-01]
user_setup:
  - service: upstash-redis
    why: "Config cache with immediate toggle invalidation (CACHE-01)"
    env_vars:
      - name: UPSTASH_REDIS_URL
        source: "Upstash Console -> Redis database -> Connect -> redis(s):// URL (free tier). Local dev falls back to REDIS_URL."
  - service: google-ai-studio
    why: "Gemini Live voice LLM (declared here so .env.example is complete; consumed in 25-03)"
    env_vars:
      - name: GOOGLE_API_KEY
        source: "Google AI Studio -> Get API key"
  - service: telnyx
    why: "Telephony provider replacing Twilio (declared here; consumed in 25-03)"
    env_vars:
      - name: TELNYX_API_KEY
        source: "Telnyx Portal -> Account -> API Keys"
      - name: TELNYX_CONNECTION_ID
        source: "Telnyx Portal -> Voice -> Call Control / TeXML Application -> Connection ID"
      - name: TELNYX_VOICE_PHONE_NUMBER
        source: "Telnyx Portal -> Numbers -> purchased number (E.164)"
  - service: pinecone
    why: "Per-tenant RAG vector store (declared here; consumed in 25-04)"
    env_vars:
      - name: PINECONE_API_KEY
        source: "Pinecone Console -> API Keys (Starter free tier)"
      - name: PINECONE_INDEX_NAME
        source: "Set to cobranza-rag (created in 25-04)"

must_haves:
  truths:
    - "tenant_configs document is upserted by user_id and read back identical (minus _id)"
    - "get_tenant_config returns from Redis on cache hit and from MongoDB on cache miss, caching with 300s TTL"
    - "invalidate_tenant_config deletes the Redis key so the next read reloads from MongoDB"
    - "upsert_tenant_config calls invalidate_tenant_config(user_id) on every successful write"
    - "toggle_module upserts {modules.<module>: enabled} and calls invalidate_tenant_config immediately after"
    - "agent_instances prompt_history keeps at most 5 entries after repeated updates"
    - "8 xfail test stubs collect and run without import errors"
  artifacts:
    - path: "backend/cobranza/config_cache.py"
      provides: "Redis cache helpers get_redis/get_tenant_config/invalidate_tenant_config"
      contains: "async def get_tenant_config"
    - path: "backend/cobranza/tenant_config.py"
      provides: "CRUD for tenant_configs + agent_instances + rag_documents metadata; includes toggle_module"
      contains: "async def upsert_tenant_config"
    - path: "backend/tests/test_cobranza_phase25.py"
      provides: "Nyquist xfail scaffold, 8 stubs (2 per req across the phase)"
      contains: "test_tenant_config_hot_reload"
    - path: "backend/requirements.txt"
      provides: "New phase-25 dependencies pinned"
      contains: "langchain-text-splitters"
  key_links:
    - from: "backend/cobranza/config_cache.py"
      to: "backend/database.py tenant_configs collection"
      via: "find_one fallback on cache miss"
      pattern: "tenant_configs.find_one"
    - from: "backend/database.py init_db"
      to: "tenant_configs / agent_instances / rag_documents indexes"
      via: "_safe_index calls"
      pattern: "_safe_index\\(db\\.tenant_configs"
---

<objective>
Establish the multi-tenant config foundation: 3 new MongoDB collections with indexes, a CRUD module, a Redis cache layer with 5-minute TTL and immediate invalidation, and the Nyquist xfail test scaffold for the whole phase. Also add all new dependencies and env vars so later waves never block on install.

Purpose: Everything in Phase 25 (voice hot-reload, sub-agents, RAG) reads tenant config through this cache. The cache-invalidation contract (CACHE-01) must work before any voice call uses it.
Output: config_cache.py, tenant_config.py, 4 new indexes in database.py, requirements.txt + .env.example updates, test_cobranza_phase25.py with 8 xfail stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-agentic-multi-tenant-architecture/RESEARCH.md
@.planning/phases/25-agentic-multi-tenant-architecture/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies, env vars, and Nyquist xfail scaffold</name>
  <read_first>
    backend/requirements.txt
    backend/.env.example
    backend/tests/test_cobranza.py
    .planning/phases/25-agentic-multi-tenant-architecture/RESEARCH.md (Validation Architecture + Wave 0 Gaps sections)
  </read_first>
  <files>backend/requirements.txt, backend/.env.example, backend/tests/test_cobranza_phase25.py</files>
  <action>
    Append to backend/requirements.txt (verify telnyx first with `pip index versions telnyx`; if it resolves, pin it):
    `pipecat-ai[google]`, `google-genai>=2.8.0`, `pinecone>=9.1.0`, `pinecone[asyncio]`, `langchain-text-splitters>=1.1.2`, `telnyx`.
    Run `pip install "pipecat-ai[google]" "pinecone>=9.1.0" "pinecone[asyncio]" "langchain-text-splitters>=1.1.2" telnyx` so the rest of the phase can import them.
    Add to backend/.env.example (with placeholder values): GOOGLE_API_KEY, TELNYX_API_KEY, TELNYX_CONNECTION_ID, TELNYX_VOICE_PHONE_NUMBER, PINECONE_API_KEY, PINECONE_INDEX_NAME=cobranza-rag, UPSTASH_REDIS_URL.
    Create backend/tests/test_cobranza_phase25.py with 8 xfail stubs (strict=False, lazy imports inside test bodies, self-contained reset_db autouse fixture duplicated from test_cobranza.py per Phase 17/23 decision). Name the stubs exactly: test_tenant_config_hot_reload, test_cache_invalidation, test_orchestrator_dispatch, test_telnyx_serializer, test_gemini_live_import, test_rag_namespace_isolation, test_search_knowledge_tenant_isolation, test_module_toggle_cache. Each marked `@pytest.mark.xfail(strict=False, reason="Phase 25 WIP")`. Stub bodies raise NotImplementedError (consistent with Phase 14 decision) — do NOT inline implementation. Use an async_client fixture with a lazy `from main import app` import inside the fixture body.
  </action>
  <verify>
    <automated>cd backend && python -c "import telnyx, pinecone; from langchain_text_splitters import RecursiveCharacterTextSplitter; print('deps ok')"</automated>
    <automated>cd backend && pytest tests/test_cobranza_phase25.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_cobranza_phase25.py` collects 8 tests, all reported as xfailed (not errors), zero collection ImportErrors.
    - requirements.txt contains all 6 new dependency lines.
    - .env.example contains all 7 new env var keys.
  </acceptance_criteria>
  <done>8 xfail stubs collect and xfail; new deps importable; env vars documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MongoDB collections + indexes + tenant_config CRUD</name>
  <read_first>
    backend/database.py (lines 1-30 _safe_index, 401-425 upsert_client_profile, 1182-1205 update_wa_session, init_db index block)
    backend/cobranza/debtor_crud.py (lines 13-23 _utcnow + _serialize)
    .planning/phases/25-agentic-multi-tenant-architecture/25-PATTERNS.md (tenant_config.py section)
  </read_first>
  <files>backend/database.py, backend/cobranza/tenant_config.py</files>
  <behavior>
    - upsert_tenant_config(user_id, fields) sets fields + updated_at, $setOnInsert created_at; re-read returns same values. MUST call invalidate_tenant_config(user_id) from config_cache at the end of every successful write (CACHE-01 contract).
    - get_tenant_config_doc(user_id) returns serialized dict (_id as str) or None.
    - upsert_agent_instance(user_id, fields) upserts model/temperature/tools_enabled.
    - append_prompt_version(user_id: str, agent_type: str, new_prompt: str): pushes {version, prompt, updated_at} then trims to last 5 in a SEPARATE update_one (never $push $slice — mongomock constraint, Phase 16 decision). After 7 calls prompt_history length == 5 and contains the last 5 prompts in order.
    - save_rag_document_metadata(user_id, filename, source_type, chunk_count) inserts into rag_documents with pinecone_namespace == user_id; returns inserted_id str.
    - toggle_module(user_id, module, enabled): does upsert_one on tenant_configs setting {f"modules.{module}": enabled} + updated_at; then calls invalidate_tenant_config(user_id) immediately after the write. Example: toggle_module("u1", "voice", False) → tenant_configs["u1"]["modules"]["voice"] == False AND Redis key deleted.
  </behavior>
  <action>
    Create backend/cobranza/tenant_config.py importing `from database import get_db` and reusing the _serialize/_utcnow helpers (copy the 2-line _utcnow and the _serialize from debtor_crud.py pattern). Implement: upsert_tenant_config(user_id: str, fields: dict), get_tenant_config_doc(user_id: str), upsert_agent_instance(user_id: str, fields: dict), get_agent_instance(user_id: str), append_prompt_version(user_id: str, agent_type: str, new_prompt: str), save_rag_document_metadata(user_id, filename, source_type, chunk_count), get_rag_documents(user_id), toggle_module(user_id: str, module: str, enabled: bool). All MongoDB ops filter by `{"user_id": user_id}`. append_prompt_version uses the exact two-op trim pattern from update_wa_session (push entry, then if len(prompt_history) > 5 set prompt_history = [-5:]); cap is 5 not 10.
    CACHE INVALIDATION CONTRACT: upsert_tenant_config MUST call `await invalidate_tenant_config(user_id)` (imported from cobranza.config_cache) at the end of every successful write. toggle_module MUST also call `await invalidate_tenant_config(user_id)` immediately after the upsert_one.
    In backend/database.py init_db(), after the existing index block, add via _safe_index: `db.tenant_configs` user_id unique=True; `db.agent_instances` user_id unique=True; `db.rag_documents` [("user_id",1),("filename",1)]; `db.rag_documents` [("user_id",1),("created_at",-1)].
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_cobranza_phase25.py -x -q</automated>
    <automated>cd backend && python -c "import ast,sys; ast.parse(open('cobranza/tenant_config.py').read()); print('parse ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: tenant_config.py contains `def append_prompt_version` and the trim uses two separate `update_one` calls (grep `update_one` returns >=2 inside that function; no `$slice` substring anywhere in the file — `grep -c '\$slice' cobranza/tenant_config.py` returns 0).
    - Source assertion: every find/update in tenant_config.py includes `"user_id"` in its filter.
    - Source assertion: tenant_config.py contains `def toggle_module` with `invalidate_tenant_config` called inside it.
    - Cache invalidation: after calling upsert_tenant_config(), `redis.get(f'tenant_config:{user_id}')` returns None (key deleted). Verify with test_cache_invalidation.
    - database.py init_db contains 4 new `_safe_index(db.tenant_configs|agent_instances|rag_documents` lines.
  </acceptance_criteria>
  <done>3 collections have indexes; CRUD module persists and reads back per user_id; prompt_history capped at 5 via two-op pattern; toggle_module invalidates Redis immediately; upsert_tenant_config invalidates Redis on every write.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Redis config cache (5min TTL + immediate invalidation)</name>
  <read_first>
    backend/cobranza/tenant_config.py (from Task 2)
    backend/arq_pool.py (redis URL parsing pattern)
    .planning/phases/25-agentic-multi-tenant-architecture/25-PATTERNS.md (Shared Patterns -> Redis Cache config_cache.py)
    .planning/phases/25-agentic-multi-tenant-architecture/RESEARCH.md (Pattern 1 + Pitfall 5 Upstash SSL)
  </read_first>
  <files>backend/cobranza/config_cache.py</files>
  <behavior>
    - get_tenant_config(user_id) returns cached dict when Redis key tenant_config:{user_id} exists (json.loads).
    - On cache miss, loads from MongoDB via get_tenant_config_doc, then setex(key, 300, json_value); returns dict (or {} when no doc).
    - invalidate_tenant_config(user_id) deletes the Redis key; subsequent get reloads from MongoDB.
    - Module-toggle scenario: write modules.voice=false in MongoDB + invalidate -> next get_tenant_config reflects voice=false (CACHE-01).
  </behavior>
  <action>
    Create backend/cobranza/config_cache.py per the PATTERNS Shared Pattern. Use `import redis.asyncio as aioredis`. get_redis() memoizes a module-level client built from `os.getenv("UPSTASH_REDIS_URL") or os.getenv("REDIS_URL", "redis://localhost:6379")` with encoding="utf-8", decode_responses=True. Note Pitfall 5 in a comment: Upstash needs rediss:// (SSL); Railway redis:// is plain. get_tenant_config(user_id) checks key `tenant_config:{user_id}`, returns json.loads on hit; on miss calls tenant_config.get_tenant_config_doc(user_id), pops _id if present, setex(key, 300, json.dumps(doc, default=str)) only when doc truthy, returns doc or {}. invalidate_tenant_config(user_id) awaits redis.delete(f"tenant_config:{user_id}"). datetime values must serialize — use `default=str` in json.dumps.
    Turn test_cache_invalidation and test_module_toggle_cache green. For tests, use fakeredis or monkeypatch get_redis to an in-memory async stub if no real Redis is available in CI — document the choice in the test.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_cobranza_phase25.py::test_cache_invalidation tests/test_cobranza_phase25.py::test_module_toggle_cache tests/test_cobranza_phase25.py::test_tenant_config_hot_reload -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: test_tenant_config_hot_reload, test_cache_invalidation, test_module_toggle_cache pass (remove xfail or assert XPASS handling per scaffold).
    - Source assertion: config_cache.py contains `setex(` with `300` and `delete(` on key `tenant_config:`.
    - Source assertion: no `aioredis` package import (must be `redis.asyncio`).
  </acceptance_criteria>
  <done>Config reads hit Redis with 5min TTL; toggle invalidation forces immediate MongoDB reload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→admin API | Config writes arrive over HTTP (guarded in 25-05) |
| API→Redis | Cached tenant config; key derived from user_id |
| API→MongoDB | Source of truth for tenant config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Spoofing | Redis key tenant_config:{user_id} | accept | user_id is a MongoDB ObjectId string; no collision possible |
| T-25-02 | Tampering | json deserialization from Redis | mitigate | only this service writes the key; json.loads of trusted server-written value |
| T-25-SC | Tampering | pip installs (pipecat-ai[google], pinecone, telnyx, langchain-text-splitters) | mitigate | telnyx is [ASSUMED] — Task 1 verifies via `pip index versions telnyx` before pinning; others approved in RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `pytest tests/test_cobranza_phase25.py -x -q` — cache + hot-reload tests green, others still xfail.
- `python -c "import telnyx, pinecone; from langchain_text_splitters import RecursiveCharacterTextSplitter"` — deps installed.
- init_db runs without index errors against a fresh test DB.
</verification>

<success_criteria>
- 3 collections indexed by user_id; CRUD persists and reads back filtered by user_id.
- prompt_history never exceeds 5 entries (two-op trim, no $slice).
- toggle_module upserts modules field and calls invalidate_tenant_config immediately.
- upsert_tenant_config calls invalidate_tenant_config on every successful write.
- get_tenant_config caches for 300s; invalidate_tenant_config forces immediate reload.
- New deps importable; env vars documented.
</success_criteria>

<output>
Create `.planning/phases/25-agentic-multi-tenant-architecture/25-01-SUMMARY.md` when done.
</output>
