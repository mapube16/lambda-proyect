---
phase: 15-pipeline-enrichment-channels
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_enrichment.py
autonomous: true
requirements:
  - ENRICH-01
  - ENRICH-02
  - ENRICH-03

must_haves:
  truths:
    - "backend/tests/test_enrichment.py exists with 7 xfail-strict stubs"
    - "Running pytest tests/test_enrichment.py -v shows 7 XFAIL results, 0 errors"
    - "Stub names exactly match the names referenced in 15-VALIDATION.md"
  artifacts:
    - path: "backend/tests/test_enrichment.py"
      provides: "xfail stubs for ENRICH-01/02/03"
      min_lines: 50
      contains: "pytest.mark.xfail(strict=True)"
  key_links:
    - from: "backend/tests/test_enrichment.py"
      to: "backend/tests/conftest.py"
      via: "shared fixtures (asyncio_mode = auto)"
      pattern: "conftest"
---

<objective>
Create the Wave 0 test scaffold for Phase 15: 7 xfail-strict stubs in backend/tests/test_enrichment.py covering ENRICH-01 (SECOP bridge), ENRICH-02 (NIT enrichment), and ENRICH-03 (WhatsApp fallback).

Purpose: Nyquist compliance — every behavior contract is documented as a failing test before any implementation touches the codebase. These stubs become green in Wave 1 plans.
Output: backend/tests/test_enrichment.py with 7 xfail stubs.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-pipeline-enrichment-channels/15-CONTEXT.md
@.planning/phases/15-pipeline-enrichment-channels/15-RESEARCH.md
@.planning/phases/15-pipeline-enrichment-channels/15-VALIDATION.md

<interfaces>
<!-- Key infrastructure the executor needs. Extracted from source. -->

From backend/tests/conftest.py (established Phase 01 pattern):
- asyncio_mode = auto (set in backend/pytest.ini)
- mongomock_motor available for DB mocking
- Existing fixtures: reset_db, client (ASGI test client)

Stub body pattern (from test_landa.py Phase 14):
```python
@pytest.mark.xfail(strict=True, reason="not implemented — Phase 15")
async def test_foo():
    raise NotImplementedError
```

The 7 test names (from 15-VALIDATION.md — use EXACTLY these names):
1. test_secop_flag_from_company_voice     (ENRICH-01)
2. test_secop_flag_fallback_to_campaign   (ENRICH-01)
3. test_nit_enrichment_saved_to_lead      (ENRICH-02)
4. test_nit_enrichment_skipped_when_no_nit (ENRICH-02)
5. test_outreach_whatsapp_sends_with_phone (ENRICH-03)
6. test_outreach_whatsapp_fallback_to_email (ENRICH-03)
7. test_outreach_no_phone_no_email         (ENRICH-03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_enrichment.py with 7 xfail stubs</name>
  <files>backend/tests/test_enrichment.py</files>
  <action>
Create backend/tests/test_enrichment.py. The file must:

1. Import pytest at the top (no other imports needed for stubs).
2. Define a module docstring grouping the 3 requirement areas.
3. Create exactly these 7 async test functions with @pytest.mark.xfail(strict=True, reason="not implemented — Phase 15") and body `raise NotImplementedError`:

   ENRICH-01 — SECOP flag resolution:
   - test_secop_flag_from_company_voice
   - test_secop_flag_fallback_to_campaign

   ENRICH-02 — NIT enrichment:
   - test_nit_enrichment_saved_to_lead
   - test_nit_enrichment_skipped_when_no_nit

   ENRICH-03 — WhatsApp fallback:
   - test_outreach_whatsapp_sends_with_phone
   - test_outreach_whatsapp_fallback_to_email
   - test_outreach_no_phone_no_email

All functions must be `async def` (asyncio_mode = auto means pytest-asyncio handles them without explicit @pytest.mark.asyncio).

DO NOT add any imports to production modules yet — stubs raise NotImplementedError immediately.
DO NOT use assert False as stub body — use raise NotImplementedError (established Phase 14 pattern).
  </action>
  <verify>
    <automated>cd /c/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_enrichment.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>pytest reports 7 xfail (strict), 0 passed, 0 errors. All test names exactly match the list above.</done>
</task>

</tasks>

<verification>
cd /c/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_enrichment.py -v

Expected output: 7 lines showing XFAIL next to each test name. Zero errors, zero failures.
</verification>

<success_criteria>
- backend/tests/test_enrichment.py exists with exactly 7 xfail-strict stubs
- pytest reports 7 XFAIL, 0 errors
- Test function names exactly match 15-VALIDATION.md names (used by Wave 1 plans' verify commands)
</success_criteria>

<output>
After completion, create .planning/phases/15-pipeline-enrichment-channels/15-01-SUMMARY.md
</output>
