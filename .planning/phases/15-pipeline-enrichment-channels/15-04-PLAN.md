---
phase: 15-pipeline-enrichment-channels
plan: 04
type: execute
wave: 2
depends_on:
  - 15-02
  - 15-03
files_modified: []
autonomous: false
requirements:
  - ENRICH-01
  - ENRICH-02
  - ENRICH-03

must_haves:
  truths:
    - "Full test suite passes: 7 enrichment tests green, 0 failures"
    - "All module imports succeed without errors"
    - "Existing tests (test_landa.py, test_senders.py, etc.) still pass — no regressions"
  artifacts:
    - path: "backend/tests/test_enrichment.py"
      provides: "7 passing tests (converted from xfail)"
      contains: "PASSED"
  key_links:
    - from: "backend/tests/test_enrichment.py"
      to: "backend/hive_tools.py + outreach.py + database.py"
      via: "pytest assertions"
      pattern: "test_"
---

<objective>
Integration smoke wave: run the full test suite to confirm all 7 enrichment tests pass, no regressions in prior test files, and all modified modules import cleanly. This is the phase gate before `/gsd:verify-work`.

Purpose: Confirm Wave 1 implementations satisfy all three requirements end-to-end.
Output: Green test suite. Human sign-off on smoke results.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-pipeline-enrichment-channels/15-02-SUMMARY.md
@.planning/phases/15-pipeline-enrichment-channels/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite — verify no regressions</name>
  <files></files>
  <action>
Run the full backend test suite from the backend directory. Collect all results.

Command: cd /c/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/ -x -q

Expected outcome:
- All 7 tests in test_enrichment.py pass (were xfail, now green)
- Tests in test_landa.py, test_senders.py, and any other existing test files pass without regression
- Zero import errors, zero collection errors

If any test fails:
1. Identify the failing test and the exception.
2. Determine if the failure is in the new code (15-02 or 15-03 changes) or a regression.
3. Fix the specific failure — do NOT refactor unrelated code.
4. Re-run until green.

Also run import smoke check for all modified modules:
python -c "
from hive_tools import make_prospecting_registry
from database import update_lead_nit_data
from landa.agents.outreach import run_outreach
from prospector import _analista_prompt, _motor_scoring_prompt
print('all imports OK')
"
  </action>
  <verify>
    <automated>cd /c/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/ -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>Full test suite green. All 7 test_enrichment.py tests pass. Zero failures, zero errors across all test files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 15 pipeline enrichment wiring:
- SECOP bridge: _discover_companies now reads fuentes_habilitadas from company_voice (ENRICH-01)
- NIT enrichment: asyncio.create_task fires after save_lead with nit_data patched to MongoDB (ENRICH-02)
- WhatsApp fallback: outreach.py falls back to email when no phone, records historial entry (ENRICH-03)
- 7 automated tests green covering all three requirements
  </what-built>
  <how-to-verify>
1. Check test results from Task 1 — confirm "7 passed" in test_enrichment.py output.

2. Optional live smoke (requires dev server): Toggle "SECOP adjudicados" on in StaffDashboard for your test user, then launch a prospecting run. In server logs, look for:
   - "[discover_companies] company_voice load" or fuentes_habilitadas being read
   - SECOP company entries appearing in discovery results

3. Check MongoDB after a prospecting run to verify a SECOP lead has a nit_data field (will appear asynchronously within 15-30 seconds of the lead being saved).

4. To test WhatsApp fallback manually: in a run where canal_elegido=whatsapp and no phone was extracted, check that the lead's historial_conversacion array contains an entry with tipo="fallback", razon="no_phone".
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 tests pass and you are satisfied with the implementation. Describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
cd /c/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/ -x -q

All tests green. Human confirms in checkpoint.
</verification>

<success_criteria>
- Full test suite: 0 failures, 0 errors
- All 7 enrichment tests converted from xfail to passing
- Human checkpoint approved
- Phase 15 ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create .planning/phases/15-pipeline-enrichment-channels/15-04-SUMMARY.md
</output>
