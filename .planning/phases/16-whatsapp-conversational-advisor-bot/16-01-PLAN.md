---
phase: 16-whatsapp-conversational-advisor-bot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/tests/test_whatsapp.py
autonomous: true
requirements: [WA-01, WA-02, WA-03, WA-04]

must_haves:
  truths:
    - "twilio package is available in requirements.txt for installation"
    - "All 4 requirements have xfail test stubs that run and are collected by pytest"
    - "Running pytest on test_whatsapp.py produces xfail outcomes, not import errors or collection failures"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "twilio>=9.0.0 dependency declaration"
      contains: "twilio>=9.0.0"
    - path: "backend/tests/test_whatsapp.py"
      provides: "xfail stubs for WA-01 through WA-04"
      exports: ["test_notify_user_routing", "test_routing_strips_whatsapp_prefix", "test_session_crud", "test_webhook_endpoint", "test_tool_call_cliente", "test_voice_note_transcription", "test_asesor_tool_dispatch"]
  key_links:
    - from: "backend/tests/test_whatsapp.py"
      to: "backend/tests/conftest.py"
      via: "autouse reset_db fixture (mongomock_motor)"
      pattern: "AsyncMongoMockClient"
---

<objective>
Install the twilio dependency and create xfail test stubs for all 4 WhatsApp requirements (WA-01 through WA-04). This Wave 0 plan is the prerequisite for all subsequent plans — it establishes the test contract and makes the Twilio package available for import in wa_handler.py.

Purpose: Nyquist-compliant TDD foundation. Every subsequent task has a failing test it must pass.
Output: backend/requirements.txt with twilio>=9.0.0 and backend/tests/test_whatsapp.py with 7 xfail stubs.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-whatsapp-conversational-advisor-bot/16-CONTEXT.md
@.planning/phases/16-whatsapp-conversational-advisor-bot/16-VALIDATION.md
@backend/requirements.txt
@backend/tests/conftest.py
@backend/tests/test_landa_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add twilio dependency and create xfail test stubs</name>
  <files>backend/requirements.txt, backend/tests/test_whatsapp.py</files>
  <action>
Two changes in one commit:

**1. backend/requirements.txt** — append after the apscheduler line:
```
twilio>=9.0.0
```
Do NOT change any existing line. The file currently ends with `apscheduler>=3.10.4` followed by a blank line — append after that.

**2. backend/tests/test_whatsapp.py** — create this file with xfail stubs for every test that later plans must make pass. Follow the exact naming convention from VALIDATION.md so `-k` filters work correctly.

The file structure:
```python
"""
test_whatsapp.py — Phase 16: WhatsApp Conversational Advisor Bot.
Tests cover WA-01 (webhook + routing), WA-02 (sessions + wa_handler),
WA-03 (tool calling + voice notes), WA-04 (asesor_interno tools).

All tests start as xfail stubs. Each plan removes xfail as it implements the feature.
"""
import pytest
import pytest_asyncio
import database
from mongomock_motor import AsyncMongoMockClient
from httpx import AsyncClient, ASGITransport
from unittest.mock import AsyncMock, patch


# ── Fixtures ──────────────────────────────────────────────────────────────────

@pytest_asyncio.fixture(autouse=True)
async def reset_db():
    """Fresh in-memory MongoDB per test — mirrors conftest.py pattern."""
    mock_client = AsyncMongoMockClient()
    await database.init_db(client=mock_client)
    yield
    await database.get_db().users.drop()


@pytest_asyncio.fixture
async def async_client():
    from main import app
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as client:
        yield client


# ── WA-01: notify_user() routing ──────────────────────────────────────────────

@pytest.mark.xfail(reason="WA-01: notify_user() not yet implemented", strict=False)
async def test_notify_user_routing_web():
    """notify_user() sends WS event when notification_channel='web'."""
    pytest.fail("not implemented")


@pytest.mark.xfail(reason="WA-01: notify_user() not yet implemented", strict=False)
async def test_notify_user_routing_whatsapp():
    """notify_user() sends WA message (no WS) when notification_channel='whatsapp'."""
    pytest.fail("not implemented")


@pytest.mark.xfail(reason="WA-01: notify_user() not yet implemented", strict=False)
async def test_notify_user_routing_both():
    """notify_user() sends WS + WA when notification_channel='both'."""
    pytest.fail("not implemented")


# ── WA-01: Webhook routing ────────────────────────────────────────────────────

@pytest.mark.xfail(reason="WA-01: /api/whatsapp/incoming not yet implemented", strict=False)
async def test_routing_strips_whatsapp_prefix(async_client):
    """Webhook strips 'whatsapp:' prefix from From before DB lookup."""
    pytest.fail("not implemented")


@pytest.mark.xfail(reason="WA-01: /api/whatsapp/incoming not yet implemented", strict=False)
async def test_routing_unknown_number_returns_twiml(async_client):
    """Unknown From number returns empty TwiML <Response/> without error."""
    pytest.fail("not implemented")


# ── WA-02: wa_sessions CRUD ───────────────────────────────────────────────────

@pytest.mark.xfail(reason="WA-02: wa_sessions CRUD not yet implemented", strict=False)
async def test_session_created_on_first_message():
    """get_or_create_wa_session() creates doc if phone not in wa_sessions."""
    pytest.fail("not implemented")


@pytest.mark.xfail(reason="WA-02: wa_sessions CRUD not yet implemented", strict=False)
async def test_session_sliding_window_max_10_turns():
    """update_wa_session() keeps only last 10 turns in history."""
    pytest.fail("not implemented")


# ── WA-02: webhook end-to-end ─────────────────────────────────────────────────

@pytest.mark.xfail(reason="WA-02: wa_handler.process_inbound() not yet implemented", strict=False)
async def test_webhook_returns_empty_twiml(async_client):
    """POST /api/whatsapp/incoming returns <Response/> immediately."""
    pytest.fail("not implemented")


# ── WA-03: LLM tool calling (cliente profile) ─────────────────────────────────

@pytest.mark.xfail(reason="WA-03: dispatch_tool_cliente() not yet implemented", strict=False)
async def test_tool_call_ver_leads_checkpoint():
    """dispatch_tool_cliente('ver_leads_checkpoint', {}, user_id) returns list."""
    pytest.fail("not implemented")


@pytest.mark.xfail(reason="WA-03: dispatch_tool_cliente() not yet implemented", strict=False)
async def test_tool_call_aprobar_lead():
    """dispatch_tool_cliente('aprobar_lead', {lead_id, canal}, user_id) returns ok."""
    pytest.fail("not implemented")


# ── WA-03: Voice note transcription ──────────────────────────────────────────

@pytest.mark.xfail(reason="WA-03: voice note transcription not yet implemented", strict=False)
async def test_voice_note_transcription_success():
    """MediaUrl0 present → httpx download → Whisper → text returned."""
    pytest.fail("not implemented")


@pytest.mark.xfail(reason="WA-03: voice note transcription not yet implemented", strict=False)
async def test_voice_note_transcription_failure_returns_fallback():
    """Whisper failure → returns fallback message (not exception)."""
    pytest.fail("not implemented")


# ── WA-04: asesor_interno tools ───────────────────────────────────────────────

@pytest.mark.xfail(reason="WA-04: dispatch_tool_asesor() not yet implemented", strict=False)
async def test_asesor_tool_buscar_licitaciones():
    """dispatch_tool_asesor('buscar_licitaciones', {sector, ciudad}, user_id) calls secop_radar."""
    pytest.fail("not implemented")


@pytest.mark.xfail(reason="WA-04: dispatch_tool_asesor() not yet implemented", strict=False)
async def test_asesor_tool_enriquecer_empresa():
    """dispatch_tool_asesor('enriquecer_empresa', {nit}, user_id) calls nit_enricher."""
    pytest.fail("not implemented")
```

The test names use the exact keywords from VALIDATION.md:
- `notify_user` → tests for WA-01 notify_user() routing
- `routing` → tests for WA-01 webhook routing
- `session` → tests for WA-02 wa_sessions CRUD
- `webhook` → tests for WA-02 webhook handler
- `tool_call` → tests for WA-03 LLM tool calling
- `voice_note` → tests for WA-03 voice note flow
- `asesor` → tests for WA-04 asesor_interno tools

All use `strict=False` so xfail is non-strict — they show as xfail in output rather than blocking collection.
  </action>
  <verify>
    <automated>cd /c/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_whatsapp.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - backend/requirements.txt contains `twilio>=9.0.0`
    - backend/tests/test_whatsapp.py exists with 14 xfail stubs
    - `pytest tests/test_whatsapp.py -v` collects all stubs and reports them as xfail (no errors, no collection failures)
    - `-k "notify_user"` matches 3 tests, `-k "routing"` matches 2, `-k "session"` matches 2, `-k "webhook"` matches 1, `-k "tool_call"` matches 2, `-k "voice_note"` matches 2, `-k "asesor"` matches 2
  </done>
</task>

</tasks>

<verification>
```
cd /c/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_whatsapp.py -v
```
All 14 stubs collected, all xfail. No errors, no import failures.

Check keyword filters individually:
```
python -m pytest tests/test_whatsapp.py -k "notify_user" -v
python -m pytest tests/test_whatsapp.py -k "routing" -v
python -m pytest tests/test_whatsapp.py -k "session" -v
python -m pytest tests/test_whatsapp.py -k "webhook" -v
python -m pytest tests/test_whatsapp.py -k "tool_call" -v
python -m pytest tests/test_whatsapp.py -k "voice_note" -v
python -m pytest tests/test_whatsapp.py -k "asesor" -v
```
Each filter matches at least one test.
</verification>

<success_criteria>
- `twilio>=9.0.0` in requirements.txt
- 14 xfail stubs in test_whatsapp.py covering all 4 requirements
- All stubs collected without import errors
- All keyword filters from VALIDATION.md match at least one test each
</success_criteria>

<output>
After completion, create `.planning/phases/16-whatsapp-conversational-advisor-bot/16-01-SUMMARY.md`
</output>
