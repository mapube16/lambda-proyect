---
phase: 13-landa-agent-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_landa_pipeline.py
autonomous: true
requirements: [LANDA-05, LANDA-06, LANDA-07, LANDA-08]
must_haves:
  truths:
    - "pytest collects test_landa_pipeline.py without import errors"
    - "All 8 stubs report xfailed (strict=True), 0 errors"
    - "Each stub documents the observable contract for its requirement"
  artifacts:
    - path: "backend/tests/test_landa_pipeline.py"
      provides: "Wave 0 xfail stubs for LANDA-05 through LANDA-08"
      contains: "pytest.mark.xfail(strict=True)"
  key_links:
    - from: "backend/tests/test_landa_pipeline.py"
      to: "backend/landa/core/context.py"
      via: "import call_agent — must not fail at collection time"
      pattern: "from landa.core.context import"
---

<objective>
Create the Wave 0 xfail test scaffold for Phase 13 — 8 strict xfail stubs, two per requirement (LANDA-05 through LANDA-08). The stubs document the observable contracts that later plans must satisfy. No implementation yet.

Purpose: Nyquist compliance — every automated verify command in plans 02-06 has a pre-existing test to flip green.
Output: backend/tests/test_landa_pipeline.py with 8 xfail stubs, pytest reports 8 xfailed, 0 errors.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-landa-foundation/12-01-SUMMARY.md
@backend/tests/test_landa.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 8 xfail stubs in test_landa_pipeline.py</name>
  <files>backend/tests/test_landa_pipeline.py</files>
  <action>
Create `backend/tests/test_landa_pipeline.py`. Follow the exact xfail pattern from `backend/tests/test_landa.py` (Phase 12 Wave 0): `@pytest.mark.xfail(reason="LANDA-XX not implemented yet", strict=True)` on async test functions. All 8 stubs must assert False as body so they report xfailed, not xpassed.

Write exactly these 8 stubs:

**LANDA-05 (Investigador scoring with sector_profile):**
- `test_investigador_returns_canales_with_probability` — scoring result must include `canales` list where each item has `canal` (str) and `probabilidad` (int 0-100); asserts `len(result["canales"]) >= 1` and `"probabilidad" in result["canales"][0]`
- `test_investigador_puntaje_in_range` — scoring returns `puntaje` between 0 and 100 inclusive; asserts `0 <= result["puntaje"] <= 100`

**LANDA-06 (Automatic routing post-scoring):**
- `test_routing_below_40_sets_rejected` — lead with puntaje < 40 gets `system_state="REJECTED_BY_AI"` and estado remains "investigando"; no estado transition happens
- `test_routing_40_to_69_transitions_to_nurturing` — lead with puntaje 45 gets `update_lead_estado(→ "nurturing")` called; asserts final estado is "nurturing"

**LANDA-07 (Outreach agent sends message):**
- `test_run_outreach_returns_true_on_success` — `run_outreach(lead_id, user_id, "email", intento=1)` returns True when SMTP mock succeeds; asserts `result is True`
- `test_run_outreach_logs_to_historial` — after `run_outreach()` completes, the lead document in MongoDB has `historial_conversacion` list with at least one entry containing `{"tipo": "outreach", "canal": "email"}`

**LANDA-08 (Nurturing agent content + re-entry detection):**
- `test_run_nurturing_returns_dict_with_required_keys` — `run_nurturing(lead_id, user_id)` returns dict with keys: `mensaje_enviado` (str), `senial_detectada` (bool), `nuevo_estado` (str)
- `test_run_nurturing_detects_reentrada_signal` — when lead's latest historial_conversacion entry contains a keyword from `sector_profile.senales_reentrada`, nurturing transitions lead to "checkpoint"; asserts returned `nuevo_estado == "checkpoint"`

The file header comment: "test_landa_pipeline.py — Wave 0 xfail stubs for Phase 13: Lead Outreach and Nurturing Agents."
  </action>
  <verify>
    <automated>cd C:/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_landa_pipeline.py -v 2>&1 | tail -5</automated>
  </verify>
  <done>pytest reports exactly 8 xfailed, 0 errors, 0 passed. All stubs collected without ImportError.</done>
</task>

</tasks>

<verification>
cd C:/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_landa_pipeline.py tests/test_landa.py -v 2>&1 | tail -10
Expect: 8 xfailed in test_landa_pipeline.py, 6 passed + 2 xfailed in test_landa.py (Phase 12 unchanged). 0 errors total.
</verification>

<success_criteria>
- test_landa_pipeline.py exists with 8 stubs
- All stubs use strict=True xfail
- pytest: 8 xfailed, 0 errors, 0 passed in that file
- No regression in test_landa.py
</success_criteria>

<output>
After completion, create `.planning/phases/13-landa-agent-pipeline/13-01-SUMMARY.md`
</output>
