---
phase: 23-intelligent-prospecting-chat-with-nl-input-and-company-knowledge-base
plan: 05
type: execute
wave: 4
depends_on: [23-03, 23-04]
files_modified: []
autonomous: false
requirements: [NL-04]
must_haves:
  truths:
    - "User opens the app, navigates to the Campaña tab, sees the NL textarea (not the old multi-turn Comenzar button)"
    - "Typing a one-line description and pressing Enter triggers a real /api/chat/prospect call and surfaces the extracted confirmation card"
    - "Knowledge Base panel expands, persists text on blur, and the learning badge updates after approving a lead"
    - "Full pytest backend suite green; frontend builds with zero TS errors"
  artifacts: []
  key_links:
    - from: backend (running locally or staging)
      to: frontend (running locally or staging)
      via: "live HTTP + cookie-auth"
      pattern: "/api/chat/prospect|/api/knowledge"
---

<objective>
End-to-end human verification that Phase 23 works in a real browser against a live backend. Backend automated tests already cover NL-01 through SIGNAL-FB-01; this checkpoint covers the manual-only verifications declared in 23-VALIDATION.md (NL-04 — visual + WebSocket end-to-end).

Purpose: Confirm the new UX actually replaces the old form, the API round-trips work with real OpenAI, and the closed-loop learning signal flows through to the badge.
Output: User approval message — phase ready for /gsd:verify-work and SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/23-intelligent-prospecting-chat-with-nl-input-and-company-knowledge-base/23-VALIDATION.md
@.planning/phases/23-intelligent-prospecting-chat-with-nl-input-and-company-knowledge-base/23-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full backend + frontend health checks before handing to user</name>
  <read_first>
    - .planning/phases/23-intelligent-prospecting-chat-with-nl-input-and-company-knowledge-base/23-VALIDATION.md (Full suite command)
  </read_first>
  <files></files>
  <action>
Run automated gates to ensure the verification reaches the human in a known-good state:

1. `cd backend && python -m pytest -x -q --timeout=30` — must exit 0
2. `cd backend && python -m pytest tests/test_prospect_chat.py -v -q` — must show 8 tests all PASSED or XPASSED (no XFAIL remaining, no FAILED)
3. `cd frontend && npx tsc --noEmit` — must exit 0
4. `cd frontend && npm run build` — must exit 0
5. Print a short summary to the user: backend tests count, frontend build size, and a reminder to start `python -m uvicorn main:app --reload` (backend) and `npm run dev` (frontend) before the checkpoint task.

If any gate fails, STOP and report which one — do not proceed to the human-verify task.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_prospect_chat.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest -x -q --timeout=30` exits 0
    - `cd backend && python -m pytest tests/test_prospect_chat.py -q` shows all 8 tests passed or xpassed (zero failures, zero remaining strict xfail)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0
    - Summary printed to console with backend pass count and frontend bundle size
  </acceptance_criteria>
  <done>Both stacks healthy; ready for human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human end-to-end verification of NL prospecting UX</name>
  <files>(no code changes — manual verification only)</files>
  <action>Pause execution and present the what-built / how-to-verify content below to the user. Wait for the resume-signal before continuing.</action>
  <verify>Human types "approved" in chat.</verify>
  <done>User has manually verified all 12 steps and signaled approval.</done>
  <what-built>
- Single-turn NL textarea on the Campaña tab (replaces the old "Comenzar →" CTA)
- Extracted Params Confirmation Card with "Parámetros extraídos" header and 8 field cells
- Knowledge Base panel (collapsible, auto-saves on blur)
- Learning indicator badge inside the KB panel header ("Sin señales aún" or "N aprobados · M rechazados")
- POST /api/chat/prospect endpoint with knowledge-base context injection
- GET/POST /api/knowledge endpoints
- Fire-and-forget signal feedback hook on POST /api/leads/{id}/decision
  </what-built>
  <how-to-verify>
1. Start backend: `cd backend && python -m uvicorn main:app --reload --port 8000`
2. Start frontend: `cd frontend && npm run dev`
3. Open http://localhost:5173 (or the printed Vite URL). Log in with a test account.
4. Navigate to the Campaña tab in AgentPanel.
5. **Verify NL input (UI-01):** confirm you see the "Configurar Campaña" heading, a 3-row textarea with placeholder "Ej: busca propietarios arrendando en Bogotá...", a gradient ➤ button, and the hint text "Describe en una frase a quién quieres prospectar". The old "Comenzar →" call-to-action is GONE.
6. **Verify NL extraction (UI-02):** type "busca propietarios arrendando en Bogotá" and press Enter. Within ~3s the textarea region is replaced by:
   - "Parámetros extraídos" header in purple
   - "N campos" count in cyan (#78dce8)
   - Grid of 8 cards showing inferred values (Industria should contain "inmobiliaria" or similar; Ciudad should contain "Bogotá")
   - "✎ Editar parámetros" toggle below — clicking expands the existing form
7. **Verify clarification path:** click "↩ Nueva campaña" to reset. Type a very ambiguous message like "hola" and submit. The fallback CampaignChat bubble UI should render with the assistant's reply.
8. **Verify Knowledge Base panel (UI-03):** click "↩ Nueva campaña" again, run the first extraction again so the post-extraction view is visible. Locate the "Base de conocimiento" panel below the form / above the slider. Click "▼ Expandir". The textarea labeled "Tu producto / ICP" should appear. Type "vendemos seguros empresariales para PyMEs en Colombia", then click outside the textarea (blur). The label should briefly read "Guardado ✓" in green for ~1.5s.
9. **Verify Knowledge Base persistence:** reload the page. Reopen Campaña tab → expand the KB panel. The textarea should be pre-filled with "vendemos seguros empresariales para PyMEs en Colombia".
10. **Verify context injection:** click "↩ Nueva campaña", type "busca empresas similares a las que ya cubrimos". The extracted params should reflect knowledge of insurance/PyME context (industria should bias toward business/PyME, not random).
11. **Verify Learning Badge (UI-04):** the badge in the KB header should currently read "Sin señales aún". Trigger a real prospecting run, wait for a lead to reach checkpoint, approve it. Reload the page → reopen KB panel. The badge should now read "1 aprobados · 0 rechazados" (or higher).
12. **Verify what did NOT break:**
    - Tab bar (Campaña / Resultados / Aprobados / Chat Reina) still works
    - Max prospects slider still adjusts
    - "INICIAR PROSPECCIÓN" button still launches a run
    - Lead cards still appear in the Resultados tab
    - CheckpointModal still opens when a lead reaches checkpoint
  </how-to-verify>
  <resume-signal>Type "approved" if all 12 steps pass. Otherwise describe which step failed and what you observed — orchestrator will route to /gsd:plan-phase --gaps.</resume-signal>
</task>

</tasks>

<verification>
- Human signs off all 12 verification steps
- Full pytest suite green
- Frontend builds clean
</verification>

<success_criteria>
Phase 23 visually and functionally complete. NL chat replaces form, knowledge base persists, signal feedback closes the loop, no regressions in existing tabs.
</success_criteria>

<output>
After completion, create `.planning/phases/23-intelligent-prospecting-chat-with-nl-input-and-company-knowledge-base/23-05-SUMMARY.md` summarizing what the human verified and any issues raised.
</output>
