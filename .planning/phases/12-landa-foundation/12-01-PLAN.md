---
phase: 12-landa-foundation
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_landa.py
autonomous: true
requirements: [LANDA-01, LANDA-02, LANDA-03, LANDA-04]

must_haves:
  truths:
    - "Test file exists with 4 xfail stubs covering every LANDA requirement"
    - "Running pytest on the stub file produces 4 XFAIL results, zero errors"
  artifacts:
    - path: "backend/tests/test_landa.py"
      provides: "xfail stubs for LANDA-01 through LANDA-04"
      contains: "@pytest.mark.xfail"
  key_links:
    - from: "backend/tests/test_landa.py"
      to: "backend/landa/"
      via: "import statements (not yet resolvable — xfail absorbs ImportError)"
      pattern: "pytest.mark.xfail"
---

<objective>
Create xfail test stubs for all 4 LANDA requirements so that Nyquist compliance is satisfied before any production code is written.

Purpose: Every subsequent wave implements code to flip these stubs from XFAIL to PASS. The stubs document the exact observable contract for each requirement.
Output: backend/tests/test_landa.py with 4 xfail async test functions.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-landa-foundation/12-CONTEXT.md

<interfaces>
<!-- Existing test patterns — extracted from backend/tests/conftest.py and test_auth.py -->

conftest.py key fixtures:
```python
# reset_db — autouse, replaces Motor client with AsyncMongoMockClient per test
# async_client — AsyncClient(ASGITransport(app=app), base_url="http://test")
```

pytest.ini:
```ini
[pytest]
asyncio_mode = auto
testpaths = tests
```

xfail pattern (from Phase 1 wave-0 precedent):
```python
@pytest.mark.xfail(reason="not implemented yet", strict=True)
async def test_something():
    assert False
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write xfail stubs for LANDA-01 through LANDA-04</name>
  <files>backend/tests/test_landa.py</files>
  <action>
Create backend/tests/test_landa.py with exactly 4 xfail async test functions. Use strict=True on all marks so they become errors once accidentally-passing code is introduced. Each stub must describe its eventual contract in its body comment.

File structure:

```python
"""
test_landa.py — Wave 0 xfail stubs for Phase 12: Landa Foundation.
Each test will be un-xfailed as the corresponding implementation lands.
"""
import pytest


# ── LANDA-01: Lead State Machine ──────────────────────────────────────────────

@pytest.mark.xfail(reason="LANDA-01 not implemented yet", strict=True)
async def test_lead_estado_valid_transition():
    """
    update_lead_estado(lead_id, user_id, "checkpoint") on a lead in state
    "investigando" must succeed and persist the new state.
    """
    from landa.state_machine import VALID_TRANSITIONS, update_lead_estado  # noqa
    assert False


@pytest.mark.xfail(reason="LANDA-01 not implemented yet", strict=True)
async def test_lead_estado_invalid_transition_raises():
    """
    update_lead_estado(lead_id, user_id, "handover") on a lead in state
    "investigando" must raise ValueError — direct jump is not a valid transition.
    """
    from landa.state_machine import update_lead_estado  # noqa
    assert False


# ── LANDA-02: sector_profiles Generation ─────────────────────────────────────

@pytest.mark.xfail(reason="LANDA-02 not implemented yet", strict=True)
async def test_generate_sector_profile_returns_schema():
    """
    generate_sector_profile("tecnologia", "Colombia", "mediana") must return a
    dict containing all required keys: decisor_primario, ganchos, objeciones,
    senales_compra, senales_reentrada, canal_principal, tono, ciclo_venta.
    """
    from landa.sector_profiles import generate_sector_profile  # noqa
    assert False


@pytest.mark.xfail(reason="LANDA-02 not implemented yet", strict=True)
async def test_generate_sector_profile_uses_cache():
    """
    Calling generate_sector_profile twice with the same args within 30 days
    must return the same document from DB without calling GPT a second time.
    """
    from landa.sector_profiles import generate_sector_profile  # noqa
    assert False


# ── LANDA-03: APScheduler + scheduled_actions ─────────────────────────────────

@pytest.mark.xfail(reason="LANDA-03 not implemented yet", strict=True)
async def test_schedule_retry_creates_job():
    """
    schedule_retry(lead_id="abc", canal="email", days=7) must insert a
    document in scheduled_actions with tipo="reintento" and estado="pendiente".
    """
    from landa.scheduler import schedule_retry  # noqa
    assert False


@pytest.mark.xfail(reason="LANDA-03 not implemented yet", strict=True)
async def test_cancel_lead_actions_removes_jobs():
    """
    After schedule_retry + schedule_nurturing for lead "abc",
    cancel_lead_actions("abc") must leave zero scheduled_actions docs for that lead_id.
    """
    from landa.scheduler import schedule_retry, schedule_nurturing, cancel_lead_actions  # noqa
    assert False


# ── LANDA-04: build_system_prompt Variable Template Builder ───────────────────

@pytest.mark.xfail(reason="LANDA-04 not implemented yet", strict=True)
async def test_build_system_prompt_replaces_all_vars():
    """
    build_system_prompt("[SECTOR] opera en [PAIS_REGION]", {"SECTOR": "tech", "PAIS_REGION": "Colombia"})
    must return "tech opera en Colombia".
    """
    from landa.core.context import build_system_prompt  # noqa
    assert False


@pytest.mark.xfail(reason="LANDA-04 not implemented yet", strict=True)
async def test_build_system_prompt_marks_missing_vars():
    """
    build_system_prompt("[SECTOR] opera en [PAIS_REGION]", {"SECTOR": "tech"})
    must return "tech opera en [inferida — PAIS_REGION]" (missing key → inferida marker).
    """
    from landa.core.context import build_system_prompt  # noqa
    assert False
```

No production imports outside the xfail bodies — the ImportError is absorbed by xfail(strict=True).
  </action>
  <verify>
    <automated>cd C:/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_landa.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>pytest reports 8 XFAIL and 0 errors/failures for tests/test_landa.py. The file exists at backend/tests/test_landa.py.</done>
</task>

</tasks>

<verification>
Run: cd backend && python -m pytest tests/test_landa.py -v
Expected: 8 xfailed, 0 passed, 0 errors
</verification>

<success_criteria>
All 8 xfail stubs collected and xfailing. The import paths (landa.state_machine, landa.sector_profiles, landa.scheduler, landa.core.context) match the module structure that Wave 1-3 plans will create.
</success_criteria>

<output>
After completion, create `.planning/phases/12-landa-foundation/12-01-SUMMARY.md`
</output>
