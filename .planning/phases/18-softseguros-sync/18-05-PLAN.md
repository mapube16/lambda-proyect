---
phase: 18-softseguros-sync
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - frontend/src/components/DebtorsSoftSegurosTab.tsx
  - frontend/src/components/CobranzaTab.tsx
  - frontend/src/components/SoftSegurosSetup.tsx
  - frontend/src/hooks/useSoftSegurosDebtors.ts
  - backend/tests/test_softseguros_integration.py
autonomous: true
requirements:
  - SOFTSEG-08
  - SOFTSEG-10

must_haves:
  truths:
    - "ClientDashboard / CobranzaTab gains a 'SOFTSEGUROS' section with onboarding form when not configured"
    - "Once configured, the section shows 2 tabs (Próximos a vencer | Ya vencidos) populated from /api/debtors?status=..."
    - "Each debtor card shows nombre, telefono, valor_a_pagar formatted as currency, fecha_pago with days remaining"
    - "Manual sync button calls POST /api/debtors/sync-now; disabled+tooltip with countdown when rate-limited (429)"
    - "SyncStatusBadge shows 'Última sync hace Nh' from /api/debtors/sync-status, refreshed on tab focus"
    - "Onboarding shows progress UI: poll /sync-status every 3s until last sync_log mode='onboarding' has completed_at"
    - "Multi-tenant E2E: two tokens, two sets of debtors, no cross-contamination"
    - "Integration test confirms full flow: configure → onboarding sync mocked → list returns expected items"
    - "All 20 xfail stubs in test_softseguros.py have turned green (xpassed) — adjust @pytest.mark.xfail decorator or remove if appropriate"
  artifacts:
    - path: "frontend/src/components/DebtorsSoftSegurosTab.tsx"
      provides: "Two-tab UI with debtor lists and manual sync button"
      min_lines: 150
    - path: "frontend/src/components/SoftSegurosSetup.tsx"
      provides: "Credential form + onboarding progress UI"
      min_lines: 80
    - path: "frontend/src/hooks/useSoftSegurosDebtors.ts"
      provides: "Hook for list/sync-status/sync-now/configure with optimistic UI"
      min_lines: 60
    - path: "backend/tests/test_softseguros_integration.py"
      provides: "End-to-end integration test with mocked SOFTSEGUROS API"
      min_lines: 80
  key_links:
    - from: "frontend/src/components/CobranzaTab.tsx"
      to: "frontend/src/components/DebtorsSoftSegurosTab.tsx"
      via: "embed the SOFTSEGUROS section inside the existing cobranza tab"
      pattern: "DebtorsSoftSegurosTab|SoftSeguros"
---

<objective>
Ship the user-facing surface: onboarding form, two-tab debtor view inside the existing cobranza dashboard, manual sync button with rate-limit feedback, sync-status badge, and an integration test that proves end-to-end flow with a mocked SOFTSEGUROS API. Close out Phase 18 by turning the 20 xfail stubs from Plan 18-01 into passing tests.
</objective>

<context>
@.planning/phases/18-softseguros-sync/18-CONTEXT.md
@frontend/src/components/CobranzaTab.tsx
@frontend/src/components/ClientDashboard.tsx
@frontend/src/hooks/useWebSocket.ts
@backend/routes/debtors.py
@backend/tests/test_softseguros.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useSoftSegurosDebtors hook</name>
  <files>frontend/src/hooks/useSoftSegurosDebtors.ts</files>
  <behavior>
    Hook returns: { setup, debtors, syncStatus, loading, error, configure, triggerSync, refetch }
    - setup: {configured: bool, configuredAt: Date|null} from GET /configure-softseguros
    - debtors: {proximosAVencer: Array, yaVencidos: Array} from GET /api/debtors?status=...
    - syncStatus: from GET /api/debtors/sync-status
    - configure(username, password): POST /configure-softseguros; on 200 starts polling sync-status until onboarding completed_at
    - triggerSync(): POST /sync-now; on 429 sets error.code='rate_limited', error.retryAfter=seconds; on 200 polls until completion
    - Refetch debtors after every successful sync completion
  </behavior>
  <action>
    Create the hook with internal useState for each slice. Use existing axios instance / fetch wrapper from the project (check ClientDashboard.tsx for the pattern). Implement polling with setInterval cleared on unmount.
  </action>
  <verify>
    <automated>cd C:/Users/maxim/Desktop/hive-pixel-office/frontend && npx tsc --noEmit 2>&amp;1 | grep -E "useSoftSegurosDebtors|error" | head -10</automated>
  </verify>
  <done>Hook compiles without TS errors. Manual smoke: render in a sandbox component, verify network tab shows the 3 calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SoftSegurosSetup component (onboarding)</name>
  <files>frontend/src/components/SoftSegurosSetup.tsx</files>
  <behavior>
    - Form with username + password fields + "Conectar SOFTSEGUROS" button
    - On submit: hook.configure(username, password)
    - During in-flight: button disabled with "Validando..."
    - On 400 (invalid creds): inline error "Credenciales inválidas"
    - On success: show "Importando deudores..." with indeterminate progress bar until sync completes
    - When sync completes: dispatch onComplete prop callback
  </behavior>
  <action>
    Standard React form. Use the pixel-art / project visual style (check LoginView.tsx for reference). NO new design system — reuse existing CSS classes.
  </action>
  <verify>
    <automated>cd C:/Users/maxim/Desktop/hive-pixel-office/frontend && npx tsc --noEmit 2>&amp;1 | grep -E "SoftSegurosSetup" | head -5</automated>
  </verify>
  <done>Compiles. Manual: render with a fake hook returning rejected configure → error renders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DebtorsSoftSegurosTab component</name>
  <files>frontend/src/components/DebtorsSoftSegurosTab.tsx</files>
  <behavior>
    - If !setup.configured: render <SoftSegurosSetup />
    - Else: render header with SyncStatusBadge + "Actualizar ahora" button + two tabs ("Próximos a vencer (N)", "Ya vencidos (N)")
    - Each tab lists DebtorCard inline (no separate component file — keep it small):
      * nombre (bold)
      * cliente_email (mailto link) | cliente_celular (tel link)
      * valor_a_pagar formatted as COP currency
      * fecha_pago + computed "Vence en X días" or "Vencido hace X días"
      * numero_factura small text
    - Empty state per tab: "No hay deudores en esta categoría"
    - Sync button: disabled with "Espera Xs" tooltip when error.code=rate_limited
    - SyncStatusBadge: "Última sync: hace 3 min" or "Sincronizando..." when is_syncing_now
  </behavior>
  <action>
    Single .tsx file. Reuse hook from Task 1. Keep DebtorCard inline as a sub-component within the same file.
  </action>
  <verify>
    <automated>cd C:/Users/maxim/Desktop/hive-pixel-office/frontend && npx tsc --noEmit 2>&amp;1 | grep -E "DebtorsSoftSeguros" | head -5</automated>
  </verify>
  <done>Compiles, renders both states (not configured vs configured), tabs switch correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Embed in CobranzaTab</name>
  <files>frontend/src/components/CobranzaTab.tsx</files>
  <behavior>
    - At the top of CobranzaTab, render <DebtorsSoftSegurosTab /> as a section ABOVE the existing cobranza UI
    - Section is collapsible/expandable (default expanded if configured, collapsed otherwise)
  </behavior>
  <action>
    Read existing CobranzaTab structure first. Insert the new component preserving the rest of the layout. Match existing visual hierarchy.
  </action>
  <verify>
    <automated>cd C:/Users/maxim/Desktop/hive-pixel-office/frontend && npx tsc --noEmit 2>&amp;1 | tail -10</automated>
  </verify>
  <done>Full project compiles. Manual: load /dashboard → cobranza tab → SOFTSEGUROS section visible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Backend integration test (mocked SOFTSEGUROS)</name>
  <files>backend/tests/test_softseguros_integration.py</files>
  <behavior>
    End-to-end flow with httpx-mock or respx mocking SOFTSEGUROS endpoints:
    1. POST /api/debtors/configure-softseguros with creds — mock /api-token-auth/ returns {token: "t1"}
    2. Mock /api/pagopoliza/?page=1 returns 3 pagopoliza pendientes, page=2 returns 0
    3. Mock /api/poliza/{id} and /api/cliente/{id} for each
    4. Wait for onboarding sync to complete (poll sync-status)
    5. GET /api/debtors?status=proximos_a_vencer returns expected items
    6. GET /api/debtors?status=ya_vencidos returns expected items
    7. POST /api/debtors/sync-now twice quickly → second is 429
    8. GET /api/debtors/{id}/verify-fresh after mocking comisionada=true → should_call=false, reason=already_paid; doc in Mongo updated
    9. Second user with different token cannot see first user's debtors
  </behavior>
  <action>
    Use respx (httpx mock lib) or pytest-httpx. Use the same fixtures from test_cobranza.py for Mongo mock. Two JWT fixtures to test multi-tenant.
  </action>
  <verify>
    <automated>cd C:/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_softseguros_integration.py -v 2>&amp;1 | tail -15</automated>
  </verify>
  <done>Integration test passes end-to-end with 0 SOFTSEGUROS calls leaking to network (all mocked).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Turn xfail stubs green</name>
  <files>backend/tests/test_softseguros.py</files>
  <behavior>
    - All 20 stubs from Plan 18-01 should now have real implementations to test against
    - For each stub: replace `raise NotImplementedError(...)` with actual assertions against the implemented modules; remove @pytest.mark.xfail decorator
  </behavior>
  <action>
    Walk each stub. Implement the assertion that matches the original behavior comment. Verify each turns green.
  </action>
  <verify>
    <automated>cd C:/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_softseguros.py -v 2>&amp;1 | grep -E "PASSED|FAILED|ERROR" | tail -25</automated>
  </verify>
  <done>20 PASSED, 0 FAILED, 0 ERROR, 0 xfailed.</done>
</task>

</tasks>

<verification>
cd C:/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_softseguros.py tests/test_softseguros_integration.py -v 2>&amp;1 | tail -30
Expected: all tests pass, total 21+ tests, 0 failures.

cd C:/Users/maxim/Desktop/hive-pixel-office/frontend && npx tsc --noEmit 2>&amp;1 | tail -5
Expected: no TS errors.
</verification>

<success_criteria>
- Corredor entra a /dashboard, ve sección SOFTSEGUROS, configura credenciales, ve sus deudores en menos de 3 minutos desde el primer login
- Manual sync funciona y respeta rate-limit
- 20 stubs green + integration test green
- Phase 17 (voice agent) puede consumir GET /api/debtors?status=ya_vencidos para su cola (no se modifica Phase 17 en este plan; solo se documenta el contrato)
</success_criteria>

<output>
.planning/phases/18-softseguros-sync/18-05-SUMMARY.md
</output>
