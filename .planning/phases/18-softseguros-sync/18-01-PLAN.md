---
phase: 18-softseguros-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_softseguros.py
autonomous: true
requirements:
  - SOFTSEG-01
  - SOFTSEG-02
  - SOFTSEG-03
  - SOFTSEG-04
  - SOFTSEG-05
  - SOFTSEG-06
  - SOFTSEG-07
  - SOFTSEG-08
  - SOFTSEG-09
  - SOFTSEG-10

must_haves:
  truths:
    - "20 xfail test stubs exist for SOFTSEG-01 through SOFTSEG-10 (2 per req)"
    - "pytest collects test_softseguros.py without import errors before backend/softseguros/ module exists"
    - "All stubs are marked strict=False so CI does not block on unimplemented features"
  artifacts:
    - path: "backend/tests/test_softseguros.py"
      provides: "Nyquist-compliant xfail stubs for all 10 SOFTSEG requirements"
      min_lines: 120
  key_links:
    - from: "backend/tests/test_softseguros.py"
      to: "backend/softseguros/adapter.py"
      via: "lazy import inside fixture body (avoids collection-time error)"
      pattern: "import.*inside.*fixture|lazy.*import"
---

<objective>
Wave 1 test scaffold for Phase 18. Creates 20 xfail stubs (2 per requirement) covering all SOFTSEG-01 through SOFTSEG-10 behaviors — following the established pattern from Phases 16 and 17.

Purpose: Nyquist compliance — every task in subsequent plans has a pre-existing automated verify target. Tests fail (xfail) now and turn green as implementation lands.
Output: backend/tests/test_softseguros.py with 20 stubs, importable without backend/softseguros/ existing.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-softseguros-sync/18-CONTEXT.md
@backend/tests/test_cobranza.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write 20 xfail stubs for SOFTSEG-01 through SOFTSEG-10</name>
  <files>backend/tests/test_softseguros.py</files>
  <behavior>
    - SOFTSEG-01a: SoftSegurosAdapter.authenticate() POSTs to /api-token-auth/ with username+password and returns token from response
    - SOFTSEG-01b: SoftSegurosAdapter sends header "Authorization: Token <x>" (NOT Bearer) on authenticated requests
    - SOFTSEG-02a: save_credentials() encrypts password with Fernet and persists to softseguros_credentials collection
    - SOFTSEG-02b: get_credentials() decrypts and returns (username, plaintext_password); never logs plaintext
    - SOFTSEG-03a: adapter.list_pagopoliza(page=1) paginates with ?page=N and returns dict with "results" array
    - SOFTSEG-03b: sync engine enriches each pagopoliza with cliente data via /api/poliza/{id} → /api/cliente/{id}
    - SOFTSEG-04a: sync respects asyncio.Semaphore(5) — never >5 concurrent requests to SOFTSEGUROS
    - SOFTSEG-04b: adapter retries on 429 with exponential backoff respecting Retry-After header
    - SOFTSEG-05a: classify_pagopoliza returns "ya_vencidos" when fecha_pago < today AND comisionada=false
    - SOFTSEG-05b: classify_pagopoliza returns "proximos_a_vencer" when fecha_pago BETWEEN today AND today+30 AND comisionada=false
    - SOFTSEG-06a: POST /api/debtors/configure-softseguros validates credentials and triggers onboarding sync in background
    - SOFTSEG-06b: POST /api/debtors/sync-now returns 429 if last manual sync was < 5 minutes ago for that user
    - SOFTSEG-07a: GET /api/debtors/{id}/verify-fresh returns should_call=false and reason="already_paid" when SOFTSEGUROS responds with comisionada=true; updates local doc to status='pagado', is_active=false
    - SOFTSEG-07b: GET /api/debtors/{id}/verify-fresh returns should_call=true with warning when SOFTSEGUROS times out (fail-open)
    - SOFTSEG-08a: GET /api/debtors?status=ya_vencidos returns only docs where status_softseguros='ya_vencidos', is_active=true, scoped to current user_id
    - SOFTSEG-08b: GET /api/debtors/{id} returns 404 when debtor belongs to different user_id (multi-tenant isolation)
    - SOFTSEG-09a: When pagopoliza disappears from SOFTSEGUROS listing, sync verifies via single GET; if 404 marks local is_active=false, status_softseguros='eliminado' (never hard-delete)
    - SOFTSEG-09b: Multiple syncs for same pagopoliza_id are idempotent — unique index (user_id, softseguros_pagopoliza_id) prevents duplicates
    - SOFTSEG-10a: GET /api/debtors/sync-status returns last_sync_at, last_sync_mode, debtors_created/updated counts from softseguros_sync_logs
    - SOFTSEG-10b: GET /api/debtors/configure-softseguros returns {configured: bool, configured_at: datetime|null}; never returns password field
  </behavior>
  <action>
    Create backend/tests/test_softseguros.py following the exact xfail pattern from backend/tests/test_cobranza.py:

    1. Module-level imports: pytest, pytest_asyncio. NO import of backend/softseguros/* modules at module level.
    2. Copy reset_db autouse fixture pattern from test_cobranza.py (self-contained mongomock isolation).
    3. async_client fixture: lazy import `from main import app` INSIDE the fixture body (avoids collection-time ImportError before backend/softseguros/ exists).
    4. Each stub: async def test_softseg_XX_YY(...) decorated with @pytest.mark.xfail(strict=False, reason="SOFTSEG-XX not implemented yet").
    5. Stub body: `raise NotImplementedError("SOFTSEG-XX: {behavior description}")`.

    Stub names (one per behavior bullet above), naming convention `test_softseg_NN_<short_id>`:
    - test_softseg_01_authenticate_post → SOFTSEG-01
    - test_softseg_01_header_uses_token_not_bearer → SOFTSEG-01
    - test_softseg_02_save_credentials_encrypts → SOFTSEG-02
    - test_softseg_02_get_credentials_decrypts → SOFTSEG-02
    - test_softseg_03_list_pagopoliza_paginates → SOFTSEG-03
    - test_softseg_03_enrich_with_cliente → SOFTSEG-03
    - test_softseg_04_semaphore_limits_concurrency → SOFTSEG-04
    - test_softseg_04_retry_on_429_with_backoff → SOFTSEG-04
    - test_softseg_05_classify_ya_vencidos → SOFTSEG-05
    - test_softseg_05_classify_proximos_a_vencer → SOFTSEG-05
    - test_softseg_06_configure_triggers_onboarding → SOFTSEG-06
    - test_softseg_06_sync_now_rate_limit → SOFTSEG-06
    - test_softseg_07_verify_fresh_already_paid → SOFTSEG-07
    - test_softseg_07_verify_fresh_fail_open → SOFTSEG-07
    - test_softseg_08_list_filtered_by_status → SOFTSEG-08
    - test_softseg_08_tenant_isolation → SOFTSEG-08
    - test_softseg_09_soft_delete_on_404 → SOFTSEG-09
    - test_softseg_09_sync_is_idempotent → SOFTSEG-09
    - test_softseg_10_sync_status_endpoint → SOFTSEG-10
    - test_softseg_10_configure_never_returns_password → SOFTSEG-10

    Do NOT import `from softseguros import ...` at module level — backend/softseguros/ does not exist yet. Pattern verified in test_cobranza.py.
  </action>
  <verify>
    <automated>cd C:/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_softseguros.py -v --tb=short 2>&amp;1 | tail -25</automated>
  </verify>
  <done>pytest collects 20 tests, all reported as XFAIL (not ERROR, not FAILED). Exit code 0.</done>
</task>

</tasks>

<verification>
cd C:/Users/maxim/Desktop/hive-pixel-office/backend && python -m pytest tests/test_softseguros.py -v 2>&amp;1 | grep -E "XFAIL|PASSED|ERROR|FAILED|collected"
Expected: "20 xfailed" in summary line, exit code 0.
</verification>

<success_criteria>
- 20 tests collected, 20 xfailed, 0 errors, 0 failures
- File importable without backend/softseguros/ package existing
- All 10 requirement IDs represented (2 stubs each)
</success_criteria>

<output>
After completion, create .planning/phases/18-softseguros-sync/18-01-SUMMARY.md
</output>
