---
phase: 26-voice-prompt-layering
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - backend/cobranza/voice_pipecat.py
  - backend/tests/test_voice_prompt_layering.py
autonomous: true
requirements: [PROMPT-LAYER-02]
must_haves:
  truths:
    - "run_bot reads voice_personality from tenant_configs via the existing get_tenant_config Redis cache (no new cache/collection)"
    - "A tenant with no voice_personality falls back to DEFAULT_PERSONALITY — never empty, never MOTOR-only"
    - "Upserting voice_personality is reflected on the next get_tenant_config call (hot-reload via existing _invalidate)"
    - "voice_prompt_layered toggle off routes run_bot to _legacy_build_system_prompt for instant rollback"
  artifacts:
    - path: "backend/cobranza/voice_pipecat.py"
      provides: "run_bot composition path: cache read -> personality fallback -> toggle branch -> build_system_prompt"
      contains: "voice_prompt_layered"
  key_links:
    - from: "backend/cobranza/voice_pipecat.py::run_bot"
      to: "backend/cobranza/config_cache.py::get_tenant_config"
      via: "tenant_config.get('voice_personality') read from cached doc already loaded at line 133"
      pattern: "voice_personality"
    - from: "backend/cobranza/voice_pipecat.py::run_bot"
      to: "backend/cobranza/voice_prompt.py::build_system_prompt"
      via: "layered branch composes personality + datos_block + MOTOR"
      pattern: "build_system_prompt\\("
---

<objective>
Wire the Plan-01 pure composition into `run_bot` (voice_pipecat.py): read `voice_personality` from the tenant_configs doc already fetched via `get_tenant_config` (line 133), fall back to `DEFAULT_PERSONALITY` when absent, and branch on the `voice_prompt_layered` toggle (layered -> build_system_prompt; off -> _legacy_build_system_prompt). Seed DPG's personality is a no-op because the in-code default IS DPG's bytes — the fallback path covers an unseeded tenant.

Purpose: Delivers PROMPT-LAYER-02 — per-tenant personality from MongoDB, hot-reloaded via the existing 5-min-TTL cache, with a never-empty fallback and an instant per-tenant rollback toggle. Reuses Phase-25 cache/CRUD wholesale; builds no new collection or cache path.

Output: Modified run_bot composition path, default-personality fallback, and the layered/legacy toggle branch; the personality-fallback and hot-reload tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-voice-prompt-layering/26-CONTEXT.md
@.planning/phases/26-voice-prompt-layering/26-RESEARCH.md

# Plan 01 created the pure composition module this plan consumes
@.planning/phases/26-voice-prompt-layering/26-01-SUMMARY.md
@backend/cobranza/voice_prompt.py

# run_bot composition site (lines 130-328) + prompt consumption (561, 646)
@backend/cobranza/voice_pipecat.py

# Cache + CRUD to REUSE (do not rebuild) — get_tenant_config reads tenant_configs only
@backend/cobranza/config_cache.py
@backend/cobranza/tenant_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-removal safety check — grep tenant_configs for live voice_system_prompt</name>
  <files>backend/cobranza/voice_pipecat.py</files>
  <action>
Per RESEARCH Assumption A2 (MEDIUM risk): before any tenant relies on the old broken override, confirm no NON-test tenant has a non-empty `voice_system_prompt`. Run a one-time read-only check against the configured Mongo (or document that only DPG/test fixtures exist if no live DB is reachable from the dev box). Use a throwaway script that queries `db.tenant_configs.find({"voice_system_prompt": {"$nin": [None, ""]}})` and prints matching `user_id`s.

If ONLY DPG (default-path) and/or test fixtures appear: record the finding in the SUMMARY and proceed — the default-personality fallback covers them. If a real non-DPG tenant has a live override: STOP and surface it in the SUMMARY as a blocker (their personality must be seeded from manual review before Plan 03 removes the read path). This task does NOT yet remove the override block (that is Plan 03); it only de-risks the removal.

This is a verification/de-risk task with no production-code change beyond an optional documenting comment near lines 319-328 marking the override block deprecated.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_voice_prompt_layering.py -q</automated>
  </verify>
  <done>SUMMARY records the grep result (which user_ids, if any, hold a non-empty voice_system_prompt) and a go/no-go for override removal in Plan 03. No test regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire layered composition + fallback + toggle into run_bot</name>
  <files>backend/cobranza/voice_pipecat.py, backend/tests/test_voice_prompt_layering.py</files>
  <behavior>
    - test_personality_fallback_to_default: a tenant_configs doc with NO voice_personality yields the DEFAULT_PERSONALITY bytes inside the composed prompt; never empty, never MOTOR-only (assert "PERSONALIDAD Y VOZ" present in composed output).
    - test_personality_hot_reload: upsert_tenant_config(user_id, {"voice_personality": X}) then get_tenant_config(user_id) returns a doc whose voice_personality == X on the next read (fakeredis + mongomock; existing _invalidate path).
    - test_toggle_off_uses_legacy: with voice_prompt_layered=False the composed system_prompt equals _legacy_build_system_prompt output for the same debtor (rollback path).
  </behavior>
  <action>
Refactor the run_bot prompt build (voice_pipecat.py lines 159-328) to consume `cobranza.voice_prompt`:

1. Keep the runtime formatting (tono line 160, monto_natural 162-168, vencimiento_str 170-174) where it is. Move the `_policy_block` body (184-216) into the call to `build_datos_block` from voice_prompt.py (or call build_datos_block which now owns that logic per Plan 01).
2. Replace the inline `system_prompt = (...)` f-string (218-317) with a composition path:
   - `personality = (tenant_config.get("voice_personality") or "").strip() or DEFAULT_PERSONALITY(tono)` — the cached doc from line 133 is reused; NO new read (PROMPT-LAYER-02; the cache reads tenant_configs only — RESEARCH "Which collection").
   - `use_layered = tenant_config.get("voice_prompt_layered", True)` (DEFAULT_LAYERED=True is safe — the diff test guarantees byte-identity). Optionally honor a global env kill-switch `VOICE_PROMPT_LAYERED_GLOBAL` (default "1"; "0" forces legacy for all) per RESEARCH belt-and-suspenders.
   - `if use_layered:` compose `build_system_prompt(personality, datos_block, MOTOR)`; `else:` `_legacy_build_system_prompt(...)`.
   This task does NOT add anti-injection validation or caps yet (that is Plan 03) — fallback here is presence-only (`or DEFAULT_PERSONALITY`). Do NOT remove the lines 319-328 override block in THIS task if it would change the toggle-on default path; the cleanest approach is to leave the legacy override block reachable only when `not use_layered` is false — but since Plan 03 removes it entirely, here you may keep it inert below the new branch OR gate it. Pick the approach that keeps test_toggle_off_uses_legacy and the Plan-01 diff semantics intact; document the choice.
3. Confirm `system_instruction=system_prompt` (line 561) and `system_prompt + greeting_instruction` (line 646) still receive the composed value unchanged.

Un-skip and implement test_personality_fallback_to_default, test_personality_hot_reload, test_toggle_off_uses_legacy using the mongomock autouse fixture + fakeredis monkeypatch pattern (test_cobranza_phase25.py lines 60-64).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_voice_prompt_layering.py::test_personality_fallback_to_default tests/test_voice_prompt_layering.py::test_personality_hot_reload tests/test_voice_prompt_layering.py::test_toggle_off_uses_legacy -x -q</automated>
  </verify>
  <done>run_bot composes via build_system_prompt when layered; falls back to DEFAULT_PERSONALITY when no personality; routes to _legacy_build_system_prompt when toggle off. Three named tests pass. Full suite green (no Phase-25 regression): `cd backend && python -m pytest -q`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant_configs (Mongo) -> run_bot prompt | voice_personality bytes cross into the system prompt; presence-check + fallback here, full validation in Plan 03 |
| Redis cache -> run_bot | cached tenant_config doc reused for personality read; isolation via existing user_id filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-leak | Information Disclosure | cross-tenant personality read | mitigate | Reuse existing `{"user_id": user_id}` filter in tenant_configs CRUD (unchanged); personality inherits tenant isolation |
| T-26-empty | Tampering / DoS | empty or MOTOR-only prompt | mitigate | Fallback `or DEFAULT_PERSONALITY(tono)` guarantees a populated personality layer on every call |
| T-26-rollback | Repudiation | inability to revert a bad rollout | mitigate | Per-tenant `voice_prompt_layered=False` + optional global kill-switch route to _legacy_build_system_prompt without redeploy |
</threat_model>

<verification>
- Personality read uses the cached doc from line 133 — confirm no second Mongo/Redis read added for personality.
- Fallback yields a non-empty composed prompt containing the PERSONALIDAD Y VOZ block.
- Hot-reload: upsert -> _invalidate -> next get_tenant_config reflects the new personality.
- Toggle off reproduces the legacy prompt exactly.
- `cd backend && python -m pytest -q` full suite green.
</verification>

<success_criteria>
- PROMPT-LAYER-02 satisfied: personality from tenant_configs via existing cache, hot-reload on next call, never empty / never MOTOR-only.
- Instant per-tenant rollback via voice_prompt_layered toggle.
- No new collection or cache path introduced.
- A2 grep finding recorded; go/no-go for Plan 03 override removal documented.
</success_criteria>

<output>
Create `.planning/phases/26-voice-prompt-layering/26-02-SUMMARY.md` when done.
</output>
