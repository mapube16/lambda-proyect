---
phase: 26-voice-prompt-layering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cobranza/voice_prompt.py
  - backend/tests/test_voice_prompt_layering.py
  - backend/tests/fixtures/legacy_voice_prompt.txt
autonomous: true
requirements: [PROMPT-LAYER-01]
must_haves:
  truths:
    - "build_system_prompt(DEFAULT_PERSONALITY, datos, MOTOR) is byte-identical to the legacy f-string for the DPG default (empty diff)"
    - "Composition emits in source order PERSONALITY -> DATOS -> MOTOR (not the MOTOR-first shorthand)"
    - "The Nyquist test file exists and runs before any production-code change"
  artifacts:
    - path: "backend/cobranza/voice_prompt.py"
      provides: "Pure layer constants (MOTOR, DEFAULT_PERSONALITY) + build_system_prompt + build_datos_block + _legacy_build_system_prompt"
      contains: "def build_system_prompt"
    - path: "backend/tests/test_voice_prompt_layering.py"
      provides: "Nyquist test scaffold for PROMPT-LAYER-01/02/03 (all named tests from 26-VALIDATION.md)"
      contains: "test_recomposed_prompt_byte_identical_to_legacy"
    - path: "backend/tests/fixtures/legacy_voice_prompt.txt"
      provides: "Frozen golden bytes of the legacy composed system_prompt for the regression anchor"
  key_links:
    - from: "backend/cobranza/voice_prompt.py::build_system_prompt"
      to: "backend/cobranza/voice_prompt.py::_legacy_build_system_prompt"
      via: "byte-equality assertion in test_recomposed_prompt_byte_identical_to_legacy"
      pattern: "build_system_prompt.*_legacy_build_system_prompt|assert new == legacy"
---

<objective>
Establish the Nyquist test scaffold, then split the hardcoded `system_prompt` (voice_pipecat.py lines 218-317) into pure, testable layer constants + composition functions in a NEW module `backend/cobranza/voice_prompt.py` — WITHOUT changing one word of content. The acceptance gate is a byte-identical diff between the recomposed default and the legacy f-string.

Purpose: This is the LOCKED zero-regression step 1+3 (split without changing content -> diff byte-identical). It MUST land before any cap/validation change (Plans 02/03). The diff IS the regression test that proves the refactor changed nothing observable.

Output: `voice_prompt.py` (MOTOR, DEFAULT_PERSONALITY, build_datos_block, build_system_prompt, _legacy_build_system_prompt), the test file with all PROMPT-LAYER-01/02/03 stubs, and the golden fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-voice-prompt-layering/26-CONTEXT.md
@.planning/phases/26-voice-prompt-layering/26-RESEARCH.md
@.planning/phases/26-voice-prompt-layering/26-VALIDATION.md

# The file being refactored — lines 159-328 hold the prompt build, _policy_block, and override
@backend/cobranza/voice_pipecat.py

# Test framework patterns to mirror (mongomock autouse fixture; fakeredis monkeypatch)
@backend/tests/conftest.py
@backend/tests/test_cobranza_phase25.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Nyquist test scaffold + golden fixture (Wave 0)</name>
  <files>backend/tests/test_voice_prompt_layering.py, backend/tests/fixtures/legacy_voice_prompt.txt</files>
  <action>
Create `backend/tests/test_voice_prompt_layering.py` with the FAKE_DEBTOR and FAKE_TONO fixtures from 26-RESEARCH.md (note: `vencimiento` MUST be the STRING "2026-01-15", never a date object, to hit the `else: str(vencimiento)` branch at voice_pipecat.py line 174 and avoid strftime locale flakiness — Pitfall 4). Define EVERY test named in 26-VALIDATION.md's Per-Task Verification Map as a stub that asserts against imports from the new `cobranza.voice_prompt` module: test_recomposed_prompt_byte_identical_to_legacy, test_composition_order, test_personality_fallback_to_default, test_personality_hot_reload, test_rejects_injection (parametrize one case per INJECTION_PATTERNS entry), test_charset_allows_spanish, test_personality_cap_rejects, test_sanitize_runtime_value, test_toggle_off_uses_legacy.

For tests whose production code does not yet exist (everything except the Task-2 diff tests), mark them `@pytest.mark.skip(reason="implemented in 26-02 / 26-03")` so this task's commit runs green while still freezing the test names. Task 2 (this plan) un-skips test_recomposed_prompt_byte_identical_to_legacy and test_composition_order; Plans 02/03 un-skip theirs.

Mirror the mongomock autouse `reset_db` fixture (conftest.py) and the fakeredis monkeypatch pattern (test_cobranza_phase25.py lines 60-64: `monkeypatch.setattr(cc, "_redis_client", fake)`) for the cache-dependent stubs.

Generate the golden fixture `backend/tests/fixtures/legacy_voice_prompt.txt` by running the CURRENT voice_pipecat.py prompt build against FAKE_DEBTOR/FAKE_TONO and capturing the exact `system_prompt` bytes (NOT including greeting_instruction — that is appended at line 646 and excluded from the diff per Pitfall 6). Use a throwaway script or python -c that imports/inlines the current f-string logic; write the result with no trailing transformation so the bytes are frozen verbatim.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_voice_prompt_layering.py -q</automated>
  </verify>
  <done>Test file exists with all 9 named tests (skipped where prod code is absent); golden fixture file exists and is non-empty; pytest collects and passes (skips allowed) with no import errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract pure layer constants + composition into voice_prompt.py (byte-identical gate)</name>
  <files>backend/cobranza/voice_prompt.py, backend/tests/test_voice_prompt_layering.py</files>
  <behavior>
    - test_recomposed_prompt_byte_identical_to_legacy: build_system_prompt(DEFAULT_PERSONALITY built with FAKE_TONO, build_datos_block(FAKE_DEBTOR), MOTOR) == _legacy_build_system_prompt(FAKE_DEBTOR, FAKE_TONO), asserted with difflib.unified_diff in the message. Empty diff.
    - test_composition_order: the composed output has PERSONALITY bytes before DATOS bytes before MOTOR bytes (assert index("PERSONALIDAD Y VOZ") < index("DATOS DE ESTA LLAMADA") < index("FLUJO DE LA CONVERSACION")).
  </behavior>
  <action>
Create `backend/cobranza/voice_prompt.py` as a pure, I/O-free module. Lift the prompt segments verbatim from voice_pipecat.py lines 219-317 into module-level pieces (per PROMPT-LAYER-01, the 3-layer split MOTOR + PERSONALITY + DATOS):

- `DEFAULT_PERSONALITY(tono: str) -> str` (or a constant template with `{tono}` replaced via str.replace, NOT str.format — locked decision, the prompt contains JSON-like braces): exact bytes of lines 219-230 (identity sentence + PERSONALIDAD Y VOZ bullets through line 230). Per RESEARCH Assumption A1, the "no diminutives" (231-232) and "señor" (233-234) lines are MOTOR, not personality.
- `MOTOR_ADDRESS_RULES`: a small constant holding lines 231-234 verbatim (the MOTOR sub-block that interleaves immediately after PERSONALITY — RESEARCH Pitfall 2). It is emitted right after personality, before DATOS, to preserve byte position.
- `build_datos_block(debtor: dict, *, debtor_name, monto_natural, vencimiento_str) -> str`: Layer-3 DATOS — the `_policy_block` builder (lines 184-216) PLUS the "DATOS DE ESTA LLAMADA" text (236-242) and "COMO HABLAR DE LA POLIZA" (244-247). Move the `_p_money`/`_p_date`/`_policy_lines` logic here verbatim. Keep all interpolation points exactly as-is so bytes match.
- `MOTOR`: exact bytes of lines 249-317 (FLUJO ... PROHIBIDO).
- `build_system_prompt(personality: str, datos_block: str, motor: str) -> str`: pure concat in SOURCE order PERSONALITY -> (MOTOR_ADDRESS_RULES) -> DATOS -> MOTOR. CRITICAL (RESEARCH Pitfall 1): do NOT reorder to "MOTOR first". Preserve every `\n\n` separator attached to its original segment (Pitfall 3).
- `_legacy_build_system_prompt(debtor, *, debtor_name, monto_natural, vencimiento_str, tono) -> str`: the CURRENT monolithic f-string (lines 218-317) lifted verbatim into a function. This is dual-use: the diff test's reference AND the rollback target Plan 02 branches to. Do NOT delete it.

Do NOT yet modify voice_pipecat.py's run_bot — that wiring is Plan 02. This task only creates the pure module and proves byte-identity. Un-skip and implement test_recomposed_prompt_byte_identical_to_legacy and test_composition_order in the test file. The golden fixture from Task 1 may be used as a second anchor (assert composed == fixture.read_text()).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_voice_prompt_layering.py::test_recomposed_prompt_byte_identical_to_legacy tests/test_voice_prompt_layering.py::test_composition_order -x -q</automated>
  </verify>
  <done>Both named tests pass: recomposed default == legacy (empty difflib diff) and composition order is PERSONALITY->DATOS->MOTOR. voice_pipecat.py run_bot is NOT yet modified. _legacy_build_system_prompt remains in the module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant config (Mongo) -> prompt composition | Tenant-supplied personality bytes will cross into the LLM system prompt (validated in Plan 03; this plan only relocates immutable code bytes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-R | Repudiation | refactor changing prompt silently | mitigate | Byte-identical diff test (test_recomposed_prompt_byte_identical_to_legacy) is the gate; golden fixture freezes legacy bytes; no behavior change can slip through |
| T-26-trunc | Tampering | MOTOR safety rules | mitigate | MOTOR lives as an in-code constant in voice_prompt.py; cannot be overridden or truncated by tenant data (override removal lands in Plan 03) |
| T-26-01-SC | Tampering | npm/pip installs | accept | Zero new packages this phase (RESEARCH: pure refactor, no Package Legitimacy Audit required) |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_voice_prompt_layering.py -q` collects all 9 named tests.
- Diff tests pass: recomposed DPG default == legacy f-string, byte-for-byte.
- Full suite has no regression introduced by the new pure module: `cd backend && python -m pytest -q`.
</verification>

<success_criteria>
- `voice_prompt.py` exists with MOTOR, DEFAULT_PERSONALITY, MOTOR_ADDRESS_RULES, build_datos_block, build_system_prompt, _legacy_build_system_prompt.
- Byte-identical diff gate is GREEN (PROMPT-LAYER-01 satisfied at the pure-function level).
- Composition emits in PERSONALITY -> DATOS -> MOTOR source order.
- Test scaffold + golden fixture exist for downstream plans to consume.
- voice_pipecat.py run_bot is untouched (wiring deferred to Plan 02).
</success_criteria>

<output>
Create `.planning/phases/26-voice-prompt-layering/26-01-SUMMARY.md` when done.
</output>
