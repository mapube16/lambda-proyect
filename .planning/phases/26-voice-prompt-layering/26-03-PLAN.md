---
phase: 26-voice-prompt-layering
plan: 03
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - backend/cobranza/voice_prompt.py
  - backend/cobranza/voice_pipecat.py
  - backend/tests/test_voice_prompt_layering.py
autonomous: true
requirements: [PROMPT-LAYER-03]
must_haves:
  truths:
    - "Personality containing an injection pattern (ignora.*instruc, code fences, </system>, [INST]) is REJECTED, not truncated"
    - "Charset whitelist allows áéíóúñ¿¡ and DEFAULT_PERSONALITY passes its own validator"
    - "Personality over 1500 chars is rejected (not silently truncated like the old [:2000] cap)"
    - "Runtime DATOS vars are sanitized: newlines, < > and code fences stripped before substitution"
    - "The broken voice_system_prompt override block (lines 319-328, [:2000] truncation) is removed"
  artifacts:
    - path: "backend/cobranza/voice_prompt.py"
      provides: "reject_injection, passes_charset, personality cap, sanitize_runtime_value"
      contains: "def reject_injection"
    - path: "backend/cobranza/voice_pipecat.py"
      provides: "compose-time validation + fallback-to-default; override block removed; runtime sanitization applied"
      contains: "reject_injection"
  key_links:
    - from: "backend/cobranza/voice_pipecat.py::run_bot"
      to: "backend/cobranza/voice_prompt.py::reject_injection"
      via: "compose-time guard: invalid/over-cap personality -> log + fall back to DEFAULT_PERSONALITY"
      pattern: "reject_injection|passes_charset"
    - from: "backend/cobranza/voice_pipecat.py::build_datos_block"
      to: "backend/cobranza/voice_prompt.py::sanitize_runtime_value"
      via: "debtor_name and _policy_block values sanitized before interpolation"
      pattern: "sanitize_runtime_value"
---

<objective>
Replace the broken 2000-char flat cap with real input validation on the personality layer: anti-injection regex (reject, not truncate), a Spanish-aware charset whitelist, a 1500-char per-layer cap, a composed-prompt token-budget guard, and runtime-variable sanitization for the DATOS layer. Remove the dead `voice_system_prompt` override block (lines 319-328).

Purpose: Delivers PROMPT-LAYER-03. This is the LOCKED zero-regression step 4 — it lands ONLY after the byte-identical gate (Plan 01) and the layered wiring (Plan 02). Validation must REJECT bad input rather than truncate it (truncation is the old bug that dropped 69% of the prompt including safety rules).

Output: Validation helpers in voice_prompt.py, compose-time + write-time enforcement and runtime sanitization in voice_pipecat.py, override block removed; all PROMPT-LAYER-03 tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-voice-prompt-layering/26-CONTEXT.md
@.planning/phases/26-voice-prompt-layering/26-RESEARCH.md

@.planning/phases/26-voice-prompt-layering/26-02-SUMMARY.md
@backend/cobranza/voice_prompt.py
@backend/cobranza/voice_pipecat.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validation + sanitization helpers to voice_prompt.py</name>
  <files>backend/cobranza/voice_prompt.py, backend/tests/test_voice_prompt_layering.py</files>
  <behavior>
    - test_rejects_injection (parametrized): each INJECTION_PATTERNS case ("ignora las instrucciones", "olvida el sistema", "```", "</system>", "[INST]", "new instructions:", "actua como", "system prompt") makes reject_injection return True.
    - test_charset_allows_spanish: passes_charset("áéíóúñ¿¡ Señor") is True AND passes_charset(DEFAULT_PERSONALITY(FAKE_TONO)) is True AND reject_injection(DEFAULT_PERSONALITY(FAKE_TONO)) is False (Pitfall 5 — the validator must not reject DPG's own default).
    - test_personality_cap_rejects: a 1501-char personality is rejected (the over-cap check returns reject), and the function does NOT return a truncated string.
    - test_sanitize_runtime_value: sanitize_runtime_value strips "\n", "\r", "<", ">", "```" and truncates to max_len; clean input passes through unchanged.
  </behavior>
  <action>
Add to voice_prompt.py (stdlib `re` only — no new dependency, RESEARCH Standard Stack):

- `INJECTION_PATTERNS` + compiled `_INJECTION_RE` (re.IGNORECASE | re.UNICODE) and `reject_injection(text) -> bool` exactly as specified in 26-RESEARCH.md "Injection-pattern rejection" (per PROMPT-LAYER-03). Treat the pattern list as an expandable baseline (Assumption A4).
- `ALLOWED_CHARS_RE` + `passes_charset(text) -> bool` from RESEARCH "Character whitelist" — MUST admit áéíóúüñ ÁÉÍÓÚÜÑ ¿¡ and the punctuation used in DEFAULT_PERSONALITY (apostrophe, colon, comma, period, hyphen, parens). Verify against the seed (Pitfall 5).
- A 1500-char personality cap: a `PERSONALITY_MAX_CHARS = 1500` constant and a `validate_personality(text) -> bool` (or returns the reason) that REJECTS — never truncates — when over cap, on injection match, or charset failure. (Write-time enforcement via Pydantic max_length is the upsert pattern; the pure reject check lives here for reuse.)
- `sanitize_runtime_value(v, max_len=120) -> str` exactly per RESEARCH "Layer-3 runtime-variable sanitization": str(v), replace "\n"/"\r" with space, strip "<"/">", strip "```", truncate to max_len, strip().
- A composed-budget heuristic `COMPOSED_MAX_CHARS = 3500 * 4` for the compose-time token guard (char/4, Assumption A3).

Un-skip and implement test_rejects_injection, test_charset_allows_spanish, test_personality_cap_rejects, test_sanitize_runtime_value.
  </action>
  <verify>
    <automated>cd backend && python -m pytest "tests/test_voice_prompt_layering.py::test_rejects_injection" tests/test_voice_prompt_layering.py::test_charset_allows_spanish tests/test_voice_prompt_layering.py::test_personality_cap_rejects tests/test_voice_prompt_layering.py::test_sanitize_runtime_value -x -q</automated>
  </verify>
  <done>All four validation tests pass, including DEFAULT_PERSONALITY passing its own validator. Reject-not-truncate confirmed (over-cap input returns reject, not a clipped string).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce validation in run_bot, sanitize DATOS, remove the broken override block</name>
  <files>backend/cobranza/voice_pipecat.py, backend/tests/test_voice_prompt_layering.py</files>
  <behavior>
    - test_recomposed_prompt_byte_identical_to_legacy (re-run, must STAY green): FAKE_DEBTOR has clean values, so sanitize_runtime_value is a no-op and the diff stays empty (RESEARCH "Diff-identity caveat"). Adding sanitization must not break Plan 01's gate.
    - Compose-time guard: a stored personality that fails reject_injection/passes_charset/cap causes run_bot to log a warning and fall back to DEFAULT_PERSONALITY (call never runs with a rejected personality, never empty).
  </behavior>
  <action>
In voice_pipecat.py run_bot (the Plan-02 composition path):

1. Tighten the personality fallback to full validation: replace the presence-only check with
   `if not personality or reject_injection(personality) or not passes_charset(personality) or len(personality) > PERSONALITY_MAX_CHARS:` -> log + `personality = DEFAULT_PERSONALITY(tono)` (RESEARCH toggle branch). A bad stored personality must fall back, never take voice down.
2. Add the compose-time budget guard: after `build_system_prompt(...)`, if `len(system_prompt) > COMPOSED_MAX_CHARS`, log + recompose with DEFAULT_PERSONALITY(tono) (never truncate mid-sentence).
3. Apply `sanitize_runtime_value` to Layer-3 runtime vars before interpolation: `debtor_name` (line 239 usage) and each `_policy_block` value (the dict fields at lines 197-215), plus `vencimiento_str` (it can be a raw string, line 174). Because FAKE_DEBTOR values are clean, the diff test stays empty — re-run it to confirm (Pitfall caveat). monto_natural is code-formatted; sanitize defensively but it is low-risk.
4. REMOVE the dead override block (lines 319-328): delete the `tenant_prompt = tenant_config.get("voice_system_prompt", "")` read, the `[:2000]` truncation, and the brand_name/debtor_name replace. The stored Mongo data is left inert (NO migration — A2 confirmed in Plan 02). Add a one-line deprecation comment noting voice_system_prompt is read-path-removed. This deletion satisfies PROMPT-LAYER-03 "replace the flat cap."
5. (Write-path) Where personality is upserted, enforce the 1500-char cap + validation so bad input is rejected at store-time (Pydantic max_length=1500 pattern, line 323 reference) — reject to caller, never truncate. If no upsert endpoint exists in this phase's scope, document that write-time validation is exposed via validate_personality for the future admin-UI phase and enforced defensively at compose-time here.

Re-run the full named-test set to confirm the Plan-01 byte-identical gate is still GREEN after sanitization is added.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_voice_prompt_layering.py -x -q</automated>
  </verify>
  <done>Override block (lines 319-328) removed; [:2000] truncation gone. Compose-time validation falls back to default on bad/over-cap personality. Runtime vars sanitized. test_recomposed_prompt_byte_identical_to_legacy STILL passes (sanitization is a no-op on clean fixture). Full suite green: `cd backend && python -m pytest -q`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant_configs (Mongo) -> system prompt | tenant-supplied personality is now validated (reject-not-truncate) at compose-time and write-time before reaching Gemini |
| debtor doc (Mongo) -> DATOS layer | runtime debtor values sanitized (newline/angle-bracket/code-fence strip) before interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-inject | Tampering / Elevation | stored personality prompt injection | mitigate | reject_injection regex at compose-time (+ write-time); reject not truncate; fallback to DEFAULT_PERSONALITY on match |
| T-26-tag | Tampering | role-tag / XML escape (</system>, [INST]) in personality | mitigate | `</` + role-tag patterns in INJECTION_PATTERNS and charset whitelist excludes raw angle brackets |
| T-26-data | Tampering | malicious debtor name/policy field with newlines/tags | mitigate | sanitize_runtime_value strips \n \r < > and code fences on all Layer-3 substitutions |
| T-26-trunc | Tampering | silent truncation hiding safety rules (the OLD bug) | mitigate | [:2000] override block removed; MOTOR is never tenant-overridable; over-budget -> fallback not truncate |
| T-26-dos | DoS | over-long prompt -> prefill latency/cost | mitigate | 1500-char personality cap + COMPOSED_MAX_CHARS (≤3500-token) budget guard |
</threat_model>

<verification>
- Each INJECTION_PATTERNS case is rejected (one test per pattern).
- DEFAULT_PERSONALITY passes passes_charset and is not rejected by reject_injection.
- Over-1500-char personality rejected, not truncated.
- sanitize_runtime_value strips \n, <, >, code fences.
- Override block removed; no `[:2000]` remains: `grep -n "\[:2000\]\|voice_system_prompt" backend/cobranza/voice_pipecat.py` shows only an inert deprecation comment.
- Byte-identical gate still green after sanitization.
- `cd backend && python -m pytest -q` full suite green.
</verification>

<success_criteria>
- PROMPT-LAYER-03 satisfied: per-layer caps + anti-injection regex + charset whitelist + runtime sanitization; reject-not-truncate.
- Broken 2000-char override path removed; data left inert (no migration).
- Composed-prompt budget guard prevents prefill-latency blowups.
- Plan-01 byte-identical regression gate remains green.
</success_criteria>

<output>
Create `.planning/phases/26-voice-prompt-layering/26-03-SUMMARY.md` when done.
</output>
